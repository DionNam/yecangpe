---
phase: 09-ui-polish-core-ux
plan: 03
type: execute
wave: 1
depends_on: ["09-02"]
files_modified:
  - apps/web/app/globals.css
  - apps/web/app/(main)/jobs/page.tsx
  - apps/web/components/jobs/job-list-table.tsx
  - apps/web/components/jobs/job-row.tsx
  - apps/web/app/(main)/jobs/[id]/page.tsx
  - apps/web/components/jobs/job-detail.tsx
  - apps/web/components/jobs/job-detail-header.tsx
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Design has bold, distinctive aesthetic direction (not generic)"
    - "Typography feels intentional and refined (not default system fonts)"
    - "Layout has generous whitespace and visual breathing room"
    - "Backgrounds create atmospheric depth (not plain white)"
    - "Interactive elements have delightful micro-interactions"
    - "Overall design feels production-grade and memorable"
  artifacts:
    - path: "apps/web/app/globals.css"
      provides: "Custom CSS with distinctive design utilities"
      contains: "grain-texture"
      min_lines: 200
    - path: "apps/web/app/(main)/jobs/page.tsx"
      provides: "Job list with editorial-style layout"
      contains: "decorative-line"
    - path: "apps/web/components/jobs/job-detail.tsx"
      provides: "Job detail with rich visual atmosphere"
      contains: "fade-in-up"
  key_links:
    - from: "apps/web/app/globals.css"
      to: "Google Fonts (Playfair Display + Work Sans)"
      via: "@import url"
      pattern: "fonts\\.googleapis\\.com"
    - from: "apps/web/app/(main)/jobs/page.tsx"
      to: "Custom utility classes"
      via: "className attributes"
      pattern: "(grain-texture|decorative-line|fade-in-up)"
---

<objective>
Transform job pages from generic functional design to distinctive, production-grade aesthetic with bold visual direction, refined typography, atmospheric backgrounds, and delightful animations.

Purpose: Close GAP-09-01 (Design Quality Below Professional Standards). Current implementation is "generic AI slop" per user feedback. Phase 9 success criterion requires design quality matching or exceeding Purple Elephant reference, which is not met.

Output: Complete visual redesign of job list and detail pages with production-ready aesthetic quality, preserving all functional code (navigation, logout, data fetching).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-polish-core-ux/09-RESEARCH.md
@.planning/phases/09-ui-polish-core-ux/09-VERIFICATION.md
@.claude/get-shit-done/references/ui-brand.md

# Existing functional code to preserve
@apps/web/components/layout/main-nav.tsx
@apps/web/components/layout/user-menu.tsx
@apps/web/app/(main)/layout.tsx

# Files to redesign (visual only)
@apps/web/app/(main)/jobs/page.tsx
@apps/web/components/jobs/job-list-table.tsx
@apps/web/components/jobs/job-row.tsx
@apps/web/app/(main)/jobs/[id]/page.tsx
@apps/web/components/jobs/job-detail.tsx
@apps/web/components/jobs/job-detail-header.tsx

# Current design system (to enhance)
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish distinctive visual foundation</name>
  <files>
    apps/web/app/globals.css
  </files>
  <action>
    Enhance globals.css to create distinctive, production-grade design foundation following ui-brand.md principles.

    **Analysis of current state:**
    - Playfair Display and Work Sans fonts already loaded ✓
    - Custom color palette with warm teal primary, coral secondary, cream backgrounds ✓
    - Basic utility classes for decorative elements exist but unused

    **Required enhancements:**

    **1. Typography refinements:**
    - Body font already set to Work Sans ✓
    - Headers already set to Playfair Display ✓
    - ADD: Fine-tune font weights for hierarchy:
      ```css
      h1, h2, h3 { font-weight: 700; }
      h4, h5, h6 { font-weight: 600; }
      strong { font-weight: 600; }
      ```
    - ADD: Letter spacing for large display text:
      ```css
      .text-display {
        letter-spacing: -0.03em;
        line-height: 1.1;
      }
      ```

    **2. Enhanced visual depth utilities:**

    Current grain-texture utility is good, but needs:
    - Stronger grain for more visible texture: baseFrequency='2.5' (not 1.5), opacity='0.05' (not 0.03)

    ADD new utilities:
    ```css
    /* Atmospheric gradient overlays */
    .gradient-mesh {
      background:
        radial-gradient(at 0% 0%, oklch(0.72 0.15 35 / 0.08) 0px, transparent 50%),
        radial-gradient(at 100% 100%, oklch(0.45 0.12 200 / 0.08) 0px, transparent 50%);
    }

    /* Subtle shadow depth */
    .shadow-soft {
      box-shadow:
        0 1px 2px oklch(0 0 0 / 0.04),
        0 4px 12px oklch(0 0 0 / 0.06);
    }

    /* Card elevation hover */
    .card-hover {
      transition: all 200ms cubic-bezier(0.4, 0, 0.2, 1);
    }
    .card-hover:hover {
      transform: translateY(-2px);
      box-shadow:
        0 4px 12px oklch(0 0 0 / 0.08),
        0 12px 24px oklch(0 0 0 / 0.12);
    }
    ```

    **3. Enhanced decorative elements:**

    Current decorative-line is good but minimal. ADD variations:
    ```css
    /* Bold accent line (for section headers) */
    .accent-line-bold::before {
      content: '';
      position: absolute;
      left: 0;
      top: -12px;
      width: 80px;
      height: 6px;
      background: linear-gradient(90deg,
        oklch(0.72 0.15 35),
        oklch(0.45 0.12 200));
      border-radius: 3px;
    }

    /* Corner flourish */
    .corner-flourish::after {
      content: '';
      position: absolute;
      right: 0;
      bottom: 0;
      width: 120px;
      height: 120px;
      background: radial-gradient(circle at bottom right,
        oklch(0.72 0.15 35 / 0.06) 0%,
        transparent 60%);
      pointer-events: none;
    }
    ```

    **4. Refined animations:**

    Current fade-in-up is good. ADD sophisticated micro-interactions:
    ```css
    /* Smooth entrance with scale */
    .scale-in {
      animation: scaleIn 0.6s cubic-bezier(0.16, 1, 0.3, 1);
    }

    @keyframes scaleIn {
      from {
        opacity: 0;
        transform: scale(0.95);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    /* Hover lift effect */
    .hover-lift {
      transition: transform 200ms cubic-bezier(0.4, 0, 0.2, 1);
    }
    .hover-lift:hover {
      transform: translateY(-4px);
    }

    /* Badge pulse animation */
    @keyframes pulse-subtle {
      0%, 100% { opacity: 1; }
      50% { opacity: 0.8; }
    }
    .animate-pulse-subtle {
      animation: pulse-subtle 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }
    ```

    **5. Enhanced spacing system:**

    ADD generous spacing utilities:
    ```css
    .container-generous {
      padding-left: 2rem;
      padding-right: 2rem;
    }
    @media (min-width: 768px) {
      .container-generous {
        padding-left: 4rem;
        padding-right: 4rem;
      }
    }
    @media (min-width: 1024px) {
      .container-generous {
        padding-left: 6rem;
        padding-right: 6rem;
      }
    }

    .section-spacious {
      padding-top: 4rem;
      padding-bottom: 4rem;
    }
    @media (min-width: 768px) {
      .section-spacious {
        padding-top: 6rem;
        padding-bottom: 6rem;
      }
    }
    ```

    **Why these changes:**
    - Grain texture with higher opacity creates tangible atmosphere (not invisible)
    - Gradient mesh adds depth without overwhelming content
    - Multiple decorative utilities enable compositional flexibility
    - Enhanced animations feel polished (not basic CSS transitions)
    - Generous spacing utilities prevent cramped layouts

    **What NOT to change:**
    - Keep existing color palette (already distinctive)
    - Keep font imports (Playfair + Work Sans already loaded)
    - Preserve all existing Tailwind base layer code
    - Don't modify dark mode variables (focus on light mode for MVP)
  </action>
  <verify>
    Check for enhanced utilities:
    ```bash
    grep -E "(gradient-mesh|shadow-soft|card-hover|accent-line-bold|scale-in|container-generous)" apps/web/app/globals.css
    ```
  </verify>
  <done>
    - grain-texture utility enhanced with stronger visibility
    - gradient-mesh, shadow-soft, card-hover utilities added
    - accent-line-bold and corner-flourish decorative utilities added
    - scale-in, hover-lift, animate-pulse-subtle animation utilities added
    - container-generous and section-spacious spacing utilities added
    - Typography refinements for display text and weights added
    - All changes additive (existing code preserved)
  </done>
</task>

<task type="auto">
  <name>Task 2: Transform job list page to editorial-style layout</name>
  <files>
    apps/web/app/(main)/jobs/page.tsx
    apps/web/components/jobs/job-list-table.tsx
    apps/web/components/jobs/job-row.tsx
  </files>
  <action>
    Redesign job list page from generic table to distinctive editorial layout with atmospheric backgrounds and generous whitespace.

    **Current problems (per GAP-09-01):**
    - Minimal container padding (feels cramped)
    - Plain white backgrounds (no atmosphere)
    - Generic table layout (no visual interest)
    - Basic hover states (no delight)

    **1. apps/web/app/(main)/jobs/page.tsx transformations:**

    REPLACE outer container:
    ```tsx
    // BEFORE: <div className="container py-8">
    // AFTER:
    <div className="grain-texture gradient-mesh min-h-screen">
      <div className="container-generous section-spacious">
    ```

    ENHANCE header section:
    ```tsx
    // BEFORE: <div className="mb-8 space-y-4">
    // AFTER:
    <div className="mb-12 space-y-6 relative">
      <div className="decorative-line fade-in-up">
        <h1 className="text-4xl md:text-5xl font-bold tracking-tight text-display">
          채용 공고
        </h1>
      </div>
      <p className="text-lg text-muted-foreground leading-relaxed max-w-2xl fade-in-up delay-100">
        한국어 가능한 외국인을 위한 채용 공고를 확인하세요
      </p>
    </div>
    ```

    ENHANCE table container:
    ```tsx
    // BEFORE: <div className="rounded-lg border">
    // AFTER:
    <div className="bg-card/80 backdrop-blur-sm rounded-xl border shadow-soft overflow-hidden fade-in-up delay-200">
    ```

    ADD visual rhythm:
    - Filters section: "mb-8" (not mb-6)
    - Pagination: "mt-8" (not mt-6)
    - Empty state: "py-20" (not py-12) with "text-lg" (not default)

    CLOSE divs properly:
    ```tsx
    </div> {/* container-generous */}
    </div> {/* grain-texture gradient-mesh */}
    ```

    **2. apps/web/components/jobs/job-list-table.tsx enhancements:**

    ENHANCE table header:
    ```tsx
    <TableHeader>
      <TableRow className="border-b border-border/50 hover:bg-transparent">
        <TableHead className="w-[120px] text-xs font-semibold text-muted-foreground uppercase tracking-wider">
          날짜
        </TableHead>
        <TableHead className="text-xs font-semibold text-muted-foreground uppercase tracking-wider">
          제목
        </TableHead>
        <TableHead className="w-[100px] text-right text-xs font-semibold text-muted-foreground uppercase tracking-wider">
          조회수
        </TableHead>
        <TableHead className="w-[100px] text-right text-xs font-semibold text-muted-foreground uppercase tracking-wider">
          관심수
        </TableHead>
      </TableRow>
    </TableHeader>
    ```

    ADD staggered animation to table body:
    ```tsx
    <TableBody>
      {posts.map((post, index) => {
        // ... existing metrics calculation ...
        return (
          <JobRow
            key={post.id}
            job={post}
            isAuthenticated={isAuthenticated}
            displayViews={displayViews}
            displayLikes={displayLikes}
            animationDelay={index * 50} // Pass animation delay
          />
        )
      })}
    </TableBody>
    ```

    **3. apps/web/components/jobs/job-row.tsx transformation:**

    ADD animationDelay prop:
    ```tsx
    interface JobRowProps {
      job: JobPost
      isAuthenticated: boolean
      displayViews: number
      displayLikes: number
      animationDelay?: number
    }

    export function JobRow({
      job,
      isAuthenticated,
      displayViews,
      displayLikes,
      animationDelay = 0
    }: JobRowProps) {
    ```

    TRANSFORM row styling:
    ```tsx
    // BEFORE: className="cursor-pointer hover:bg-muted/50 transition-colors duration-150 group"
    // AFTER:
    <TableRow
      onClick={() => handleRowClick(job.id, isAuthenticated)}
      className="
        cursor-pointer
        border-b border-border/30 last:border-0
        hover:bg-primary/5
        transition-all duration-200
        group
        card-hover
        fade-in-up
      "
      style={{ animationDelay: `${animationDelay}ms` }}
    >
    ```

    ENHANCE title cell with more visual interest:
    ```tsx
    <TableCell>
      <div className="space-y-1">
        <h3 className="font-semibold text-base group-hover:text-primary transition-colors duration-200">
          {job.title}
        </h3>
        <p className="text-sm text-muted-foreground">
          {job.company_name}
        </p>
      </div>
    </TableCell>
    ```

    ENHANCE badge with pulse animation:
    ```tsx
    <Badge
      variant={job.hiring_status === 'hiring' ? 'default' : 'secondary'}
      className={job.hiring_status === 'hiring' ? 'animate-pulse-subtle' : ''}
    >
      {job.hiring_status === 'hiring' ? '채용중' : '마감'}
    </Badge>
    ```

    ENHANCE metrics display:
    ```tsx
    <TableCell className="text-right">
      <span className="text-sm font-medium text-muted-foreground group-hover:text-foreground transition-colors">
        {displayViews.toLocaleString()}
      </span>
    </TableCell>
    <TableCell className="text-right">
      <span className="text-sm font-medium text-muted-foreground group-hover:text-foreground transition-colors">
        {displayLikes.toLocaleString()}
      </span>
    </TableCell>
    ```

    **Why these changes:**
    - grain-texture + gradient-mesh creates atmospheric depth (not plain white)
    - container-generous + section-spacious provides generous whitespace
    - Larger header text (4xl/5xl) with decorative-line adds editorial character
    - Card with backdrop-blur and shadow-soft creates layered depth
    - Staggered fade-in-up animations create orchestrated entrance (not instant load)
    - Enhanced row hover with card-hover adds subtle lift on interaction
    - Pulse animation on "채용중" badge draws attention to active listings
    - Company name under title creates hierarchy without extra column
    - All functional logic preserved (data fetching, filtering, pagination)

    **Pattern reference:** Editorial job board design (like Dribble job listings or Behance job posts)
  </action>
  <verify>
    Check for design enhancements:
    ```bash
    grep -E "(grain-texture|gradient-mesh|container-generous|decorative-line|fade-in-up)" apps/web/app/(main)/jobs/page.tsx
    grep -E "(card-hover|animate-pulse-subtle)" apps/web/components/jobs/job-row.tsx
    ```
  </verify>
  <done>
    - Job list page has atmospheric background with grain texture and gradient mesh
    - Header section uses decorative-line and large display typography (4xl/5xl)
    - Container has generous padding (container-generous + section-spacious)
    - Table wrapped in card with backdrop blur and soft shadow
    - Job rows have enhanced hover states with subtle lift effect
    - Staggered fade-in animations create orchestrated page load
    - Badge pulses on "채용중" status for attention
    - Company name displayed under title for better hierarchy
    - All data fetching and filtering logic preserved
  </done>
</task>

<task type="auto">
  <name>Task 3: Transform job detail page to immersive editorial layout</name>
  <files>
    apps/web/app/(main)/jobs/[id]/page.tsx
    apps/web/components/jobs/job-detail.tsx
    apps/web/components/jobs/job-detail-header.tsx
  </files>
  <action>
    Redesign job detail page from basic layout to immersive editorial experience with rich visual atmosphere.

    **Current problems (per GAP-09-01):**
    - Plain container (no atmospheric background)
    - Minimal visual details (no depth or texture)
    - Basic typography (lacks editorial character)
    - No page load animations (instant, not orchestrated)

    **1. apps/web/app/(main)/jobs/[id]/page.tsx transformation:**

    REPLACE outer container:
    ```tsx
    // BEFORE: <div className="container py-8">
    // AFTER:
    <div className="grain-texture gradient-mesh min-h-screen">
      <div className="container-generous section-spacious">
    ```

    ENHANCE inner wrapper:
    ```tsx
    // BEFORE: <div className="max-w-4xl mx-auto">
    // AFTER:
    <div className="max-w-4xl mx-auto">
      <div className="bg-card/90 backdrop-blur-sm rounded-2xl border shadow-soft overflow-hidden corner-flourish">
    ```

    CLOSE divs properly:
    ```tsx
        </div> {/* corner-flourish card */}
      </div> {/* max-w-4xl */}
    </div> {/* container-generous */}
    </div> {/* grain-texture gradient-mesh */}
    ```

    **2. apps/web/components/jobs/job-detail-header.tsx enhancements:**

    ENHANCE header structure with dramatic presentation:
    ```tsx
    <div className="relative space-y-6 p-8 md:p-12 pb-8 border-b border-border/50">
      {/* Back navigation with style */}
      <Link
        href="/jobs"
        className="inline-flex items-center gap-2 text-sm text-muted-foreground hover:text-primary transition-colors group"
      >
        <ArrowLeft className="w-4 h-4 group-hover:-translate-x-1 transition-transform" />
        <span>채용 공고로 돌아가기</span>
      </Link>

      {/* Title with decorative element */}
      <div className="space-y-4 relative decorative-line fade-in-up">
        <h1 className="text-4xl md:text-5xl font-bold tracking-tight text-display leading-tight">
          {job.title}
        </h1>

        {/* Company name with visual distinction */}
        <div className="flex items-center gap-3 fade-in-up delay-100">
          <div className="w-12 h-12 rounded-lg bg-primary/10 flex items-center justify-center">
            <Building2 className="w-6 h-6 text-primary" />
          </div>
          <div>
            <p className="text-lg font-medium text-foreground">
              {job.company_name}
            </p>
            <p className="text-sm text-muted-foreground">
              {job.company_location || '위치 미공개'}
            </p>
          </div>
        </div>

        {/* Badges and metadata */}
        <div className="flex flex-wrap items-center gap-3 fade-in-up delay-200">
          <Badge
            variant={job.hiring_status === 'hiring' ? 'default' : 'secondary'}
            className={`text-sm px-4 py-1.5 ${job.hiring_status === 'hiring' ? 'animate-pulse-subtle' : ''}`}
          >
            {job.hiring_status === 'hiring' ? '채용중' : '마감'}
          </Badge>

          <Badge variant="outline" className="text-sm px-4 py-1.5">
            {job.target_nationality === 'ANY'
              ? '국적 무관'
              : job.target_nationality}
          </Badge>

          {/* Metrics with icons */}
          <div className="flex items-center gap-4 text-sm text-muted-foreground ml-auto">
            <span className="flex items-center gap-1.5">
              <Eye className="w-4 h-4" />
              {displayViews.toLocaleString()}
            </span>
            <span className="flex items-center gap-1.5">
              <Heart className="w-4 h-4" />
              {displayLikes.toLocaleString()}
            </span>
          </div>
        </div>
      </div>

      {/* Like button with prominent placement */}
      {canLike && (
        <div className="fade-in-up delay-300">
          <LikeButton
            postId={job.id}
            initialIsLiked={isLiked}
            className="w-full sm:w-auto"
          />
        </div>
      )}
    </div>
    ```

    Import required icons:
    ```tsx
    import { ArrowLeft, Building2, Eye, Heart } from 'lucide-react'
    ```

    **3. apps/web/components/jobs/job-detail.tsx enhancements:**

    ENHANCE main content structure:
    ```tsx
    <div className="p-8 md:p-12 space-y-8 fade-in-up delay-400">
      {/* Job description section */}
      <section className="space-y-4">
        <h2 className="text-2xl font-bold relative accent-line-bold pt-4">
          공고 내용
        </h2>
        <div className="prose prose-slate max-w-none">
          <p className="text-base leading-relaxed text-foreground/90 whitespace-pre-wrap">
            {job.content}
          </p>
        </div>
      </section>

      {/* Requirements section if exists */}
      {job.requirements && (
        <section className="space-y-4 pt-8 border-t border-border/50 fade-in-up delay-500">
          <h2 className="text-2xl font-bold relative accent-line-bold pt-4">
            자격 요건
          </h2>
          <div className="prose prose-slate max-w-none">
            <p className="text-base leading-relaxed text-foreground/90 whitespace-pre-wrap">
              {job.requirements}
            </p>
          </div>
        </section>
      )}

      {/* Application info section */}
      <section className="space-y-4 pt-8 border-t border-border/50 fade-in-up delay-600">
        <h2 className="text-2xl font-bold relative accent-line-bold pt-4">
          지원 방법
        </h2>
        <div className="bg-muted/50 rounded-xl p-6 space-y-3">
          <p className="text-sm text-muted-foreground">
            이 공고에 관심이 있으시면 하트 버튼을 눌러주세요.
            구인자가 관심 표시를 확인하고 연락 드릴 예정입니다.
          </p>
          {job.contact_info && (
            <div className="flex items-start gap-2 text-sm">
              <Mail className="w-4 h-4 mt-0.5 text-primary" />
              <div>
                <p className="font-medium text-foreground">문의 연락처</p>
                <p className="text-muted-foreground">{job.contact_info}</p>
              </div>
            </div>
          )}
        </div>
      </section>

      {/* Published date footer */}
      <div className="pt-8 border-t border-border/50 text-sm text-muted-foreground text-center fade-in-up delay-700">
        게시일: {new Date(job.published_at || job.created_at).toLocaleDateString('ko-KR')}
      </div>
    </div>
    ```

    Import required icons:
    ```tsx
    import { Mail } from 'lucide-react'
    ```

    **Why these changes:**
    - grain-texture + gradient-mesh background creates immersive atmosphere
    - Rounded-2xl card with backdrop blur and corner flourish adds sophistication
    - Dramatic header with large display typography (4xl/5xl) commands attention
    - Company card with icon provides visual distinction
    - Back link with animated arrow improves navigation UX
    - Section headers with accent-line-bold add editorial character
    - Staggered fade-in animations create orchestrated reveal (not instant dump)
    - Application info in muted card draws attention without overwhelming
    - Generous padding (p-12) prevents cramped feeling
    - All functional logic preserved (like button, metrics, auth checks)

    **Pattern reference:** Medium article layout, Notion page aesthetics
  </action>
  <verify>
    Check for design enhancements:
    ```bash
    grep -E "(grain-texture|gradient-mesh|corner-flourish|rounded-2xl)" apps/web/app/(main)/jobs/[id]/page.tsx
    grep -E "(accent-line-bold|fade-in-up|delay-[0-9])" apps/web/components/jobs/job-detail.tsx
    grep -E "(decorative-line|text-display|Building2)" apps/web/components/jobs/job-detail-header.tsx
    ```
  </verify>
  <done>
    - Job detail page has atmospheric background with grain texture and gradient mesh
    - Content card has rounded-2xl with backdrop blur, soft shadow, and corner flourish
    - Header uses decorative-line and large display typography (4xl/5xl)
    - Back navigation link has animated arrow on hover
    - Company info displayed in styled card with icon
    - Section headers use accent-line-bold for editorial character
    - Staggered fade-in animations create orchestrated content reveal
    - Application info in visually distinct muted card
    - Generous padding throughout (p-12) prevents cramped layout
    - All like button, metrics, and auth logic preserved
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete visual redesign of job list and detail pages with distinctive aesthetic direction:
    - Atmospheric backgrounds with grain texture and gradient meshes
    - Editorial-style typography with Playfair Display headers and refined hierarchy
    - Generous whitespace with container-generous and section-spacious utilities
    - Rich visual details (decorative lines, corner flourishes, backdrop blur)
    - Orchestrated page load animations with staggered fade-ins
    - Delightful micro-interactions (card hover lift, badge pulse, animated icons)
  </what-built>
  <how-to-verify>
    Compare redesigned pages against GAP-09-01 acceptance criteria and verify production-grade quality:

    **1. Job List Page (https://potenhire.vercel.app/jobs):**

    **Bold aesthetic direction:**
    - [ ] Page has distinct visual identity (not generic)
    - [ ] Background has subtle grain texture (visible but not overwhelming)
    - [ ] Gradient mesh creates atmospheric depth (subtle corner gradients)
    - [ ] Overall design feels intentional and curated

    **Distinctive typography:**
    - [ ] Page title is large, bold, dramatic (Playfair Display, 4xl/5xl)
    - [ ] Headers use serif font (Playfair Display) with refined weights
    - [ ] Body text uses sans-serif (Work Sans) with comfortable sizes
    - [ ] Clear typographic hierarchy throughout
    - [ ] Decorative accent line above main title

    **Generous spacing:**
    - [ ] Container has 64-80px horizontal padding on desktop
    - [ ] Section has 64-96px vertical padding
    - [ ] Header section has ample spacing (48px bottom margin)
    - [ ] Cards and elements have breathing room
    - [ ] Layout feels spacious and uncrampedRich visual details:**
    - [ ] Table card has backdrop blur effect
    - [ ] Soft shadow on table container (not harsh drop shadow)
    - [ ] Job rows have subtle hover lift effect
    - [ ] "채용중" badge has subtle pulse animation
    - [ ] Company name displayed under job title in rows

    **Sophisticated animations:**
    - [ ] Page elements fade in with staggered timing (not instant load)
    - [ ] Header fades in first, then description, then table
    - [ ] Job rows animate in sequentially (cascade effect)
    - [ ] Hover states smooth with 200ms transitions
    - [ ] Back arrow animates on hover

    **Overall quality:**
    - [ ] Design feels production-ready (not prototype)
    - [ ] Visual quality matches or exceeds Purple Elephant reference
    - [ ] Page is memorable and distinctive
    - [ ] No generic "AI slop" feeling

    **2. Job Detail Page (https://potenhire.vercel.app/jobs/[any-id]):**

    **Immersive layout:**
    - [ ] Page has atmospheric background matching list page
    - [ ] Content in rounded card with backdrop blur
    - [ ] Corner flourish visible (subtle gradient in bottom-right)
    - [ ] Max-width constrains reading for comfort

    **Dramatic header:**
    - [ ] Job title is large, bold, commanding (4xl/5xl Playfair)
    - [ ] Company displayed in styled card with building icon
    - [ ] Back link has animated arrow on hover
    - [ ] Badges prominent with pulse on "채용중"
    - [ ] Decorative accent line above title

    **Editorial content:**
    - [ ] Section headers have accent line decoration
    - [ ] Body text has comfortable line height (leading-relaxed)
    - [ ] Clear visual separation between sections
    - [ ] Application info in visually distinct card
    - [ ] Content reveals with staggered animations

    **Visual richness:**
    - [ ] Icons used meaningfully (building, eye, heart, mail, arrow)
    - [ ] Muted background card for application info
    - [ ] Generous padding throughout (48px+)
    - [ ] Typography creates clear reading hierarchy
    - [ ] Published date in footer

    **3. Cross-page consistency:**
    - [ ] Color palette consistent (teal primary, coral secondary, cream bg)
    - [ ] Typography system consistent (Playfair headers, Work Sans body)
    - [ ] Animation timing consistent (200ms transitions, staggered fades)
    - [ ] Spacing rhythm consistent (generous padding throughout)
    - [ ] Navigation header integrates seamlessly

    **4. Functional preservation:**
    - [ ] Navigation and logout still work (from 09-01)
    - [ ] Job filtering and sorting still work
    - [ ] Pagination still works
    - [ ] Like button still works
    - [ ] View count incrementing still works
    - [ ] Authentication protection still works
    - [ ] No console errors
    - [ ] No broken layouts on mobile

    **5. Production-grade checklist (GAP-09-01 closure):**

    Compare against original gap criteria:

    **Typography (Critical):**
    - [x] NOT using generic default fonts
    - [x] HAS distinctive font choices (Playfair Display + Work Sans)
    - [x] HAS typographic hierarchy and character
    - Result: RESOLVED ✓

    **Spacing & Layout (Critical):**
    - [x] NOT minimal container padding
    - [x] HAS generous whitespace (64-80px containers)
    - [x] NOT generic table layout
    - [x] HAS creative composition (editorial style)
    - Result: RESOLVED ✓

    **Visual Details (Critical):**
    - [x] NOT plain white backgrounds
    - [x] HAS textures, gradients, visual depth
    - [x] HAS decorative elements creating brand character
    - Result: RESOLVED ✓

    **Animations & Motion (High):**
    - [x] NOT only basic hover transitions
    - [x] HAS page load reveals and staggered animations
    - [x] HAS micro-interactions that delight
    - Result: RESOLVED ✓

    **Overall Aesthetic (Critical):**
    - [x] NOT generic "AI slop" appearance
    - [x] HAS bold aesthetic direction
    - [x] Feels distinctive and memorable
    - Result: RESOLVED ✓

    **Acceptance criteria:**
    All 5 critical/high priority issues from GAP-09-01 must be resolved. Design must feel production-grade and match/exceed Purple Elephant reference quality.

    **What to look for as red flags:**
    - Animations not visible or too subtle
    - Grain texture invisible
    - Spacing still feels cramped
    - Typography still looks generic
    - Design still feels unfinished or prototype-quality
    - Layouts break on mobile
    - Functional features broken
  </how-to-verify>
  <resume-signal>
    Type "approved" if design quality now meets production standards and GAP-09-01 is fully closed, or describe specific remaining visual issues (which gap criteria still not met, what looks wrong, what needs adjustment).
  </resume-signal>
</task>

</tasks>

<verification>
Overall plan verification:

1. **Visual Foundation:**
   - globals.css enhanced with distinctive utilities
   - Grain texture, gradient mesh, shadow utilities added
   - Decorative elements (accent lines, corner flourish) available
   - Animation utilities (scale-in, hover-lift, pulse) functional
   - Generous spacing utilities (container-generous, section-spacious) implemented

2. **Job List Transformation:**
   - Atmospheric background with texture and gradients
   - Editorial typography with large display headers
   - Card-based table with backdrop blur and shadow
   - Staggered entrance animations
   - Enhanced hover states with lift effects
   - Badge pulse animation on active listings

3. **Job Detail Transformation:**
   - Immersive background matching list page
   - Dramatic header with large typography
   - Company card with icon and info
   - Section headers with decorative accents
   - Orchestrated content reveal animations
   - Application info in styled card

4. **Design Quality (GAP-09-01 Resolution):**
   - Typography: Distinctive Playfair + Work Sans ✓
   - Spacing: Generous 64-80px containers ✓
   - Visual details: Textures, gradients, decorations ✓
   - Animations: Orchestrated page load, micro-interactions ✓
   - Overall aesthetic: Bold, memorable, production-grade ✓

5. **Functional Preservation:**
   - All data fetching logic intact
   - Navigation and logout preserved
   - Filtering, sorting, pagination work
   - Like button functional
   - Authentication protection maintained
   - Metrics calculation preserved
</verification>

<success_criteria>
Phase 09 Plan 03 (Gap Closure) is complete when:

- [ ] globals.css enhanced with distinctive design utilities
- [ ] Job list page has atmospheric background (grain-texture + gradient-mesh)
- [ ] Job list header uses decorative-line and large display typography
- [ ] Job list container uses container-generous and section-spacious
- [ ] Job list table wrapped in card with backdrop blur and shadow
- [ ] Job rows have staggered fade-in animations
- [ ] Job rows have card-hover lift effect on interaction
- [ ] "채용중" badge has pulse animation
- [ ] Job detail page has atmospheric background matching list
- [ ] Job detail content in rounded-2xl card with corner flourish
- [ ] Job detail header uses decorative-line and dramatic typography
- [ ] Job detail sections have accent-line-bold decorations
- [ ] Job detail content reveals with staggered animations
- [ ] Company info displayed in styled card with icon
- [ ] Application info in visually distinct muted card
- [ ] Back navigation has animated arrow
- [ ] All functional features work (navigation, filtering, like, auth)
- [ ] Design quality matches or exceeds Purple Elephant reference
- [ ] GAP-09-01 acceptance criteria met (5/5 issues resolved)
- [ ] Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-polish-core-ux/09-03-SUMMARY.md` following the summary template.
</output>
