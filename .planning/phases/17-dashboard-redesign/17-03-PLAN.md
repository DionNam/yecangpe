---
phase: 17-dashboard-redesign
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - apps/web/components/dashboard/rich-text-editor.tsx
  - apps/web/app/(main)/dashboard/post-job/page.tsx
  - apps/web/components/employer/job-post-form.tsx
  - apps/web/app/actions/jobs.ts
autonomous: true

must_haves:
  truths:
    - "Employer can access /dashboard/post-job to create a new job post"
    - "Job post form includes a rich text editor (Tiptap) for the content/description field"
    - "Rich text editor supports bold, italic, bullet list, ordered list formatting"
    - "Created job post has expires_at set to 30 days from creation date"
    - "Job creation success redirects to /dashboard"
    - "Admin manipulation fields (review_status, view_target, like_target) are set correctly on creation"
  artifacts:
    - path: "apps/web/components/dashboard/rich-text-editor.tsx"
      provides: "Tiptap rich text editor wrapper component"
      min_lines: 50
    - path: "apps/web/app/(main)/dashboard/post-job/page.tsx"
      provides: "Job post creation page at /dashboard/post-job"
      min_lines: 20
    - path: "apps/web/components/employer/job-post-form.tsx"
      provides: "Updated form using RichTextEditor for content field"
  key_links:
    - from: "apps/web/components/employer/job-post-form.tsx"
      to: "apps/web/components/dashboard/rich-text-editor.tsx"
      via: "Component import"
      pattern: "RichTextEditor"
    - from: "apps/web/app/actions/jobs.ts"
      to: "expires_at"
      via: "Date calculation in createJobPost"
      pattern: "expires_at.*toISOString"
---

<objective>
Create the job post creation page at /dashboard/post-job with a Tiptap rich text editor for the description field. Install Tiptap packages, build the editor component, integrate it into the existing job post form, and add expires_at calculation to the create action.

Purpose: Employers need a rich text editing experience for job descriptions instead of a plain textarea. The /dashboard/post-job route replaces the old /employer/new-post route.
Output: Working post-job page with Tiptap editor, all PRD fields, and 30-day expiration logic.
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dashboard-redesign/17-RESEARCH.md
@.planning/phases/17-dashboard-redesign/17-01-SUMMARY.md

Key existing files:
@apps/web/components/employer/job-post-form.tsx (existing form - update content field)
@apps/web/app/actions/jobs.ts (existing createJobPost action - add expires_at)
@apps/web/app/(main)/employer/new-post/page.tsx (old route - already redirects from 17-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tiptap and create rich text editor component</name>
  <files>apps/web/components/dashboard/rich-text-editor.tsx</files>
  <action>
**1. Install Tiptap packages:**

Run: `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm --filter @repo/web add @tiptap/react @tiptap/pm @tiptap/starter-kit @tiptap/extension-placeholder`

**2. Create `apps/web/components/dashboard/rich-text-editor.tsx`** - Client component ('use client'):

Props interface:
```typescript
interface RichTextEditorProps {
  content: string
  onChange: (html: string) => void
  placeholder?: string
  disabled?: boolean
}
```

Implementation:
- Use `useEditor` hook from `@tiptap/react` with:
  - `StarterKit` extension (configure heading levels [2, 3])
  - `Placeholder` extension with configurable placeholder text (default: "직무 설명을 작성하세요...")
- Set `editable: !disabled`
- On `onUpdate`, call `onChange(editor.getHTML())`
- Use `useEffect` to sync external content changes (when `content` prop changes and differs from editor state, call `editor.commands.setContent(content)`)

Toolbar with shadcn/ui Button (variant="ghost", size="sm"):
- Bold (Lucide `Bold` icon) - toggleBold
- Italic (Lucide `Italic` icon) - toggleItalic
- Bullet list (Lucide `List` icon) - toggleBulletList
- Ordered list (Lucide `ListOrdered` icon) - toggleOrderedList
- H2 heading (text "H2") - toggleHeading({ level: 2 })
- H3 heading (text "H3") - toggleHeading({ level: 3 })

Each toolbar button shows active state with `cn(editor.isActive('bold') && 'bg-slate-200')`.

Editor container styling:
- Outer div: `border rounded-lg overflow-hidden bg-white`
- Toolbar: `flex items-center gap-1 p-2 bg-slate-50 border-b`
- EditorContent: Apply `prose prose-sm max-w-none focus:outline-none min-h-[300px] p-4` via `editorProps.attributes.class`

Return `null` if editor is not initialized yet.

**IMPORTANT**: The `'use client'` directive MUST be at the top of the file. Tiptap uses React hooks and context.
  </action>
  <verify>Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm --filter @repo/web build 2>&1 | tail -20` to verify Tiptap installs and component compiles. Check `pnpm --filter @repo/web exec -- ls node_modules/@tiptap/react` to confirm package installation.</verify>
  <done>Tiptap packages installed. RichTextEditor component created with toolbar (bold, italic, lists, headings) and controlled content prop.</done>
</task>

<task type="auto">
  <name>Task 2: Create post-job page and integrate Tiptap into job form</name>
  <files>
    apps/web/app/(main)/dashboard/post-job/page.tsx
    apps/web/components/employer/job-post-form.tsx
    apps/web/app/actions/jobs.ts
  </files>
  <action>
**1. Create `apps/web/app/(main)/dashboard/post-job/page.tsx`:**

Server component that:
- Authenticates user (same pattern as old /employer/new-post/page.tsx)
- Checks employer role via employer_profiles query
- Renders the JobPostForm component inside a Card layout
- Gets company_name from employer profile and passes as `defaultCompanyName` prop

```typescript
import { redirect } from 'next/navigation'
import { createClient } from '@repo/supabase/server'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { JobPostForm } from '@/components/employer/job-post-form'

export const metadata = {
  title: '새 공고 작성',
}

export default async function PostJobPage() {
  // ... same auth/role check as old new-post page
  // Render JobPostForm inside Card
}
```

**2. Update `apps/web/components/employer/job-post-form.tsx`:**

Replace the `<Textarea>` for the `content` field with the `<RichTextEditor>` component:

- Add import: `import { RichTextEditor } from '@/components/dashboard/rich-text-editor'`
- Find the FormField for `content` (the one with `<Textarea>`)
- Replace the `<Textarea>` with:
```tsx
<RichTextEditor
  content={field.value}
  onChange={field.onChange}
  placeholder="직무, 자격 요건, 복리후생 등을 작성하세요"
  disabled={isPending}
/>
```

- The form submission still uses FormData. The content field value (HTML string from Tiptap) gets submitted via the hidden input or form.handleSubmit. Since the current form uses `react-hook-form`, the `field.onChange` updates the form state, and on submit the HTML content is included in formData via `formData.append('content', data.content)`.

- Update the redirect after successful creation: change `router.push('/employer/posts')` (or wherever it currently redirects) to `router.push('/dashboard')`.

**3. Update `apps/web/app/actions/jobs.ts` - `createJobPost` function:**

Add `expires_at` calculation before the insert:
```typescript
// Calculate 30-day expiration
const expiresAt = new Date()
expiresAt.setDate(expiresAt.getDate() + 30)
```

Add to the insert object:
```typescript
expires_at: expiresAt.toISOString(),
```

This ensures all new job posts automatically expire after 30 days.

Also update the `revalidatePath` calls in both `createJobPost` and `updateJobPost`:
- Add `revalidatePath('/dashboard')` alongside existing revalidations
  </action>
  <verify>Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm --filter @repo/web build 2>&1 | tail -20` to verify build succeeds. Check that /dashboard/post-job/page.tsx exists and job-post-form.tsx imports RichTextEditor.</verify>
  <done>Post-job page renders at /dashboard/post-job. Job form uses Tiptap rich text editor for content field. New posts get 30-day expires_at. Success redirects to /dashboard.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @repo/web build` succeeds without errors
2. Tiptap packages are installed (@tiptap/react, @tiptap/pm, @tiptap/starter-kit, @tiptap/extension-placeholder)
3. RichTextEditor component renders with formatting toolbar
4. /dashboard/post-job page renders job creation form with Tiptap editor
5. Content field stores HTML from Tiptap (not plain text)
6. New job posts include expires_at (30 days from creation)
7. Successful creation redirects to /dashboard
</verification>

<success_criteria>
- Rich text editor renders in job post form with bold/italic/list/heading controls
- Job creation form at /dashboard/post-job works end-to-end
- expires_at is automatically calculated and stored for new posts
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-redesign/17-03-SUMMARY.md`
</output>
