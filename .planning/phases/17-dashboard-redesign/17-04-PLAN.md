---
phase: 17-dashboard-redesign
plan: 04
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - apps/web/components/dashboard/seeker-dashboard.tsx
  - apps/web/components/dashboard/seeker-liked-jobs.tsx
  - apps/web/components/dashboard/seeker-profile-section.tsx
  - apps/web/app/(main)/dashboard/page.tsx
  - apps/web/app/actions/profile.ts
autonomous: true

must_haves:
  truths:
    - "Seeker sees a tabbed dashboard with Liked Jobs, Profile tabs"
    - "Liked Jobs tab shows list of hearted jobs with title, company, status, and link to job detail"
    - "Profile tab shows seeker info (nationality, TOPIK level, occupation) with edit functionality"
    - "Profile edit saves and revalidates /dashboard"
  artifacts:
    - path: "apps/web/components/dashboard/seeker-dashboard.tsx"
      provides: "Tabbed seeker dashboard shell"
      min_lines: 30
    - path: "apps/web/components/dashboard/seeker-liked-jobs.tsx"
      provides: "Liked jobs list with job card links"
      min_lines: 30
    - path: "apps/web/components/dashboard/seeker-profile-section.tsx"
      provides: "Profile display and edit functionality"
      min_lines: 30
  key_links:
    - from: "apps/web/app/(main)/dashboard/page.tsx"
      to: "apps/web/components/dashboard/seeker-dashboard.tsx"
      via: "Component import"
      pattern: "SeekerDashboard"
    - from: "apps/web/components/dashboard/seeker-profile-section.tsx"
      to: "apps/web/app/actions/profile.ts"
      via: "Server action"
      pattern: "updateSeekerProfile"
---

<objective>
Build the seeker dashboard with liked jobs display and profile management. Replace the seeker placeholder from Plan 17-01 with the full SeekerDashboard component.

Purpose: Job seekers need to see their liked jobs, manage their profile information, and prepare for job alert settings (added in Plan 17-05). This replaces the old /my-page with a modern dashboard experience.
Output: Working seeker dashboard with Liked Jobs tab and Profile tab.
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dashboard-redesign/17-RESEARCH.md
@.planning/phases/17-dashboard-redesign/17-01-SUMMARY.md
@.planning/phases/17-dashboard-redesign/17-02-SUMMARY.md

Key existing files:
@apps/web/components/my-page/liked-jobs-tab.tsx (existing liked jobs - reference for patterns)
@apps/web/components/my-page/profile-tab.tsx (existing profile tab - reference for patterns)
@apps/web/components/my-page/profile-edit-modal.tsx (existing edit modal - REUSE directly)
@apps/web/app/actions/profile.ts (existing profile action - update revalidation path)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seeker dashboard components</name>
  <files>
    apps/web/components/dashboard/seeker-dashboard.tsx
    apps/web/components/dashboard/seeker-liked-jobs.tsx
    apps/web/components/dashboard/seeker-profile-section.tsx
  </files>
  <action>
**1. Create `apps/web/components/dashboard/seeker-liked-jobs.tsx`** - Client component ('use client'):

Redesigned version of the existing `liked-jobs-tab.tsx`, showing liked jobs as cards instead of a table (better for the new dashboard design).

Props:
```typescript
interface LikedJob {
  id: string
  created_at: string
  post: JobPost | null
}
interface SeekerLikedJobsProps {
  jobs: LikedJob[]
}
```

Implementation:
- If no jobs, show empty state: "하트를 누른 공고가 없습니다. 관심 있는 공고에 하트를 눌러보세요!" with a link to `/jobs` ("채용 공고 둘러보기")
- Display liked jobs as a vertical list of cards (not table). Each card shows:
  - Job title as link to `/jobs/{slug || id}` (font-medium, hover:underline)
  - Company name (text-sm text-muted-foreground)
  - `hiring_status` badge: 채용중 (default badge) or 마감 (secondary badge)
  - Date liked (from the like's `created_at`, formatted as relative date with `formatDistanceToNow` from date-fns with Korean locale)
- Cards use `div` with `border rounded-lg p-4 hover:bg-slate-50 transition-colors` styling
- Show count in section heading: "관심 공고 ({count})"

Import `formatDistanceToNow` from `date-fns` and `ko` locale from `date-fns/locale`.

**Do NOT use `getDisplayMetrics()`** - no fake metrics in the seeker dashboard. Only show the job info that matters to seekers.

**2. Create `apps/web/components/dashboard/seeker-profile-section.tsx`** - Client component ('use client'):

Modernized version of the existing `profile-tab.tsx`, reusing the existing `ProfileEditModal`.

Props:
```typescript
interface SeekerProfileSectionProps {
  profile: {
    nationality: string
    topik_level: number | null
    occupation: string | null
    referral_source: string | null
  }
}
```

Implementation:
- Same layout as existing profile-tab.tsx but with updated styling to match dashboard design
- Display profile fields in 2-column grid (nationality, TOPIK level, occupation, referral source)
- Each field in a card-like container (bg-slate-50 border rounded-lg p-4)
- "수정" button that opens the existing `ProfileEditModal` from `@/components/my-page/profile-edit-modal`
- Import `NATIONALITIES` from `@repo/lib` to display nationality name

This is essentially a copy of the existing `profile-tab.tsx` pattern, just placed in the new dashboard context.

**3. Create `apps/web/components/dashboard/seeker-dashboard.tsx`** - Client component ('use client'):

Props:
```typescript
interface SeekerDashboardProps {
  profile: SeekerProfile
  likedJobs: LikedJob[]
  alerts: JobAlert[]
}
```

Implementation:
- Dashboard header: "구직자 대시보드" heading
- Stats summary: Liked jobs count, Active alerts count (from alerts.filter(a => a.is_active).length)
- Tabbed layout using shadcn/ui Tabs (defaultValue="liked-jobs"):
  - Tab 1: "관심 공고" - renders `<SeekerLikedJobs>` with likedJobs
  - Tab 2: "프로필" - renders `<SeekerProfileSection>` with profile
  - Tab 3: "잡 알림" - placeholder div with text "잡 알림 기능 준비 중" (will be implemented in Plan 17-05)

Use same Tabs styling pattern as the employer dashboard (consistent look).
  </action>
  <verify>Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm --filter @repo/web build 2>&1 | tail -20` to verify build. Check all 3 files exist.</verify>
  <done>Seeker dashboard components created: SeekerDashboard with tabs, SeekerLikedJobs card list, SeekerProfileSection with edit modal integration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire SeekerDashboard into /dashboard route and update profile action</name>
  <files>
    apps/web/app/(main)/dashboard/page.tsx
    apps/web/app/actions/profile.ts
  </files>
  <action>
**1. Update `apps/web/app/(main)/dashboard/page.tsx`:**

Replace the seeker placeholder with the real SeekerDashboard component:

- Add import: `import { SeekerDashboard } from '@/components/dashboard/seeker-dashboard'`
- In the seeker branch (where seekerProfile is found):
  - Keep existing data fetching for liked jobs
  - Ensure liked jobs query selects enough fields for the card display. Update the liked jobs select to include more fields from job_posts:
    ```
    supabase.from('likes').select(`
      id,
      created_at,
      post:job_posts(id, title, slug, company_name, hiring_status)
    `).eq('user_id', user.id).order('created_at', { ascending: false })
    ```
  - Keep the job alerts query
  - Replace placeholder with:
    ```tsx
    return <SeekerDashboard profile={seekerProfile} likedJobs={likedJobs || []} alerts={alerts || []} />
    ```

**2. Update `apps/web/app/actions/profile.ts`:**

Update `revalidatePath` from `/my-page` to `/dashboard`:
```typescript
revalidatePath('/dashboard')
```

This ensures the seeker dashboard refreshes after profile updates.
  </action>
  <verify>Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm --filter @repo/web build 2>&1 | tail -20` to verify build.</verify>
  <done>SeekerDashboard rendered in /dashboard route for seekers. Profile updates revalidate /dashboard. Liked jobs show with full card data.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @repo/web build` succeeds
2. Seeker dashboard shows tabbed interface (Liked Jobs, Profile, Job Alerts placeholder)
3. Liked jobs display as card list with job title, company, status
4. Profile section shows seeker info with edit button
5. Profile edit modal works and saves changes
6. Job alerts tab shows placeholder text
</verification>

<success_criteria>
- Seeker visiting /dashboard sees liked jobs, profile, and alerts placeholder
- Liked jobs link to correct job detail pages using slug
- Profile edit modal works and revalidates /dashboard
- No fake metrics used anywhere in seeker dashboard
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard-redesign/17-04-SUMMARY.md`
</output>
