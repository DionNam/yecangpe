---
phase: 11-work-location-type-country-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00007_add_work_location_fields.sql
  - packages/supabase/src/types.ts
  - packages/lib/src/constants/countries.ts
  - packages/lib/src/index.ts
autonomous: true

must_haves:
  truths:
    - "Database has work_location_type ENUM with remote, hybrid, on_site values"
    - "Database has work_location_country TEXT column (nullable)"
    - "TypeScript types include work_location_type and work_location_country"
    - "COUNTRIES constant available for import across all apps"
    - "Existing job posts have work_location_type set to on_site"
  artifacts:
    - path: "supabase/migrations/00007_add_work_location_fields.sql"
      provides: "work_location_type ENUM and job_posts columns"
      min_lines: 30
    - path: "packages/lib/src/constants/countries.ts"
      provides: "COUNTRIES array with Korean names and country codes"
      exports: ["COUNTRIES", "CountryCode", "getCountryName"]
    - path: "packages/supabase/src/types.ts"
      provides: "Updated job_posts types with new fields"
      contains: "work_location_type"
  key_links:
    - from: "packages/lib/src/constants/countries.ts"
      to: "packages/lib/src/index.ts"
      via: "export statement"
      pattern: "export.*COUNTRIES"
    - from: "supabase/migrations/00007_add_work_location_fields.sql"
      to: "packages/supabase/src/types.ts"
      via: "type generation"
      pattern: "work_location_type.*work_location_country"
---

<objective>
Create database foundation for work location type and country selection.

Purpose: Enable job posts to specify whether positions are remote, hybrid, or on-site, with country selection for on-site roles. This allows foreign job seekers to filter by work location and find positions matching their location preferences.

Output: Database migration with ENUM type and columns, regenerated TypeScript types, and COUNTRIES constant matching existing NATIONALITIES pattern.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-work-location-type-country-selection/11-RESEARCH.md

# Reference files for patterns
@packages/lib/src/constants/nationalities.ts
@supabase/migrations/00001_create_base_schema.sql
@supabase/migrations/00006_add_job_images.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for work location fields</name>
  <files>supabase/migrations/00007_add_work_location_fields.sql</files>
  <action>
Create migration following the pattern from 00006_add_job_images.sql:

1. Create work_location_type ENUM with values: 'remote', 'hybrid', 'on_site'
2. Add work_location_type column to job_posts with DEFAULT 'on_site' NOT NULL
3. Add work_location_country TEXT column (nullable)
4. Drop DEFAULT after initial migration (so new posts must specify)
5. Add index on work_location_type for filter performance
6. Add composite index on (work_location_type, work_location_country) WHERE work_location_country IS NOT NULL

Follow RESEARCH.md "Migration: Add Work Location Fields" example exactly. Use 'on_site' as default for existing rows (most jobs are on-site in Korea). Include comprehensive comments explaining each section.

DO NOT use separate ENUMs for each type - single ENUM with all three values.
  </action>
  <verify>
Check migration file exists and has correct structure:
```bash
cat supabase/migrations/00007_add_work_location_fields.sql | grep -E "CREATE TYPE work_location_type|ALTER TABLE.*job_posts|CREATE INDEX"
```

Count should show: 1 ENUM creation, 2 ALTER TABLE statements, 2 CREATE INDEX statements.
  </verify>
  <done>
Migration file created with work_location_type ENUM, two new job_posts columns, DEFAULT dropped after initial set, and two indexes for filtering performance.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate TypeScript types</name>
  <files>packages/supabase/src/types.ts</files>
  <action>
Regenerate Supabase types to include work_location_type and work_location_country fields.

First, apply the migration to the database:
```bash
cd /Users/2303-pc02/potenlab/hire_foreigner
npx supabase db push
```

Then regenerate types:
```bash
npx supabase gen types typescript --local > packages/supabase/src/types.ts
```

If Supabase CLI is unavailable (Docker not running), manually add fields to packages/supabase/src/types.ts following the pattern from 10-02 (image_url field addition):
- Add to Row type: `work_location_type: Database["public"]["Enums"]["work_location_type"]; work_location_country: string | null`
- Add to Insert type: `work_location_type: Database["public"]["Enums"]["work_location_type"]; work_location_country?: string | null`
- Add to Update type: `work_location_type?: Database["public"]["Enums"]["work_location_type"]; work_location_country?: string | null`
- Add to Enums section: `work_location_type: "remote" | "hybrid" | "on_site"`

Use the auto-generated format from existing types.ts as reference.
  </action>
  <verify>
Verify types.ts includes work_location fields:
```bash
grep -A 2 "work_location" packages/supabase/src/types.ts | head -10
```

Check that job_posts Row, Insert, Update types include work_location_type and work_location_country fields.
  </verify>
  <done>
TypeScript types updated with work_location_type (ENUM) and work_location_country (string | null) fields in job_posts Row, Insert, and Update types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create COUNTRIES constant</name>
  <files>
packages/lib/src/constants/countries.ts
packages/lib/src/index.ts
  </files>
  <action>
Create packages/lib/src/constants/countries.ts following NATIONALITIES pattern exactly:

1. Copy structure from packages/lib/src/constants/nationalities.ts
2. Use COUNTRIES array with { code, name, nameEn } structure
3. Include countries from RESEARCH.md example (East Asia, Southeast Asia, South Asia, Central Asia, Western countries)
4. Add `as const` assertion for type safety
5. Export CountryCode type: `export type CountryCode = typeof COUNTRIES[number]['code']`
6. Export getCountryName helper function: `export function getCountryName(code: string): string { return COUNTRIES.find(c => c.code === code)?.name || code }`

Include 26 countries from RESEARCH.md covering major work locations for foreign nationals (Korea, Japan, China, Taiwan, Southeast Asia, South Asia, Central Asia, major Western countries).

Then update packages/lib/src/index.ts to export COUNTRIES, CountryCode, and getCountryName alongside NATIONALITIES export.

Korean names MUST be accurate - reference RESEARCH.md "Constants: Country List" section which has verified Korean translations.
  </action>
  <verify>
Check COUNTRIES constant structure and exports:
```bash
grep -E "export const COUNTRIES|export type CountryCode|export function getCountryName" packages/lib/src/constants/countries.ts
grep "COUNTRIES" packages/lib/src/index.ts
```

Verify 26 countries in array:
```bash
grep -c "{ code:" packages/lib/src/constants/countries.ts
```

Should show 26 country entries.
  </verify>
  <done>
COUNTRIES constant created with 26 countries (Korean + English names), CountryCode type exported, getCountryName helper function created, and all exported from packages/lib/src/index.ts for use across web and admin apps.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify foundation is ready:

1. Migration file exists and follows ENUM pattern from 00001 schema:
```bash
ls -lh supabase/migrations/00007_add_work_location_fields.sql
```

2. TypeScript types include new fields:
```bash
grep "work_location" packages/supabase/src/types.ts | wc -l
```
Should show multiple occurrences (Row, Insert, Update, Enums).

3. COUNTRIES constant available for import:
```bash
grep -E "COUNTRIES|getCountryName" packages/lib/src/index.ts
```

4. No TypeScript errors:
```bash
npm run build --workspace=packages/lib
```
</verification>

<success_criteria>
Foundation complete when:
- [ ] Migration creates work_location_type ENUM with 3 values
- [ ] Migration adds work_location_type column with DEFAULT for existing rows
- [ ] Migration adds nullable work_location_country column
- [ ] Migration includes performance indexes
- [ ] TypeScript types include work_location_type and work_location_country
- [ ] COUNTRIES constant matches NATIONALITIES pattern
- [ ] COUNTRIES has 26 countries with accurate Korean names
- [ ] getCountryName helper function exported
- [ ] All exports available from @repo/lib
- [ ] packages/lib builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-work-location-type-country-selection/11-01-SUMMARY.md`
</output>
