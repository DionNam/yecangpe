---
phase: 03-job-seeker-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/00004_rpc_functions.sql
  - apps/web/middleware.ts
  - apps/web/app/(main)/layout.tsx
  - apps/web/app/(main)/jobs/page.tsx
  - apps/web/components/jobs/job-list-table.tsx
  - apps/web/components/jobs/job-list-filters.tsx
  - apps/web/components/jobs/job-list-pagination.tsx
  - apps/web/components/jobs/login-modal.tsx
  - apps/web/components/ui/table.tsx
  - apps/web/components/ui/badge.tsx
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/pagination.tsx
  - apps/web/components/ui/skeleton.tsx
autonomous: true

must_haves:
  truths:
    - "Non-authenticated users can view the job list page at /jobs"
    - "Job list shows status (hiring/closed), title, date, view count, like count"
    - "Nationality filter filters posts by target_nationality including ANY"
    - "Pagination shows 10 items per page with page navigation"
    - "URL reflects current filter and page state"
    - "Clicking a job row when not logged in opens login modal"
  artifacts:
    - path: "supabase/migrations/00004_rpc_functions.sql"
      provides: "RPC functions for view count increment and like count"
      contains: "CREATE OR REPLACE FUNCTION increment_view_count"
    - path: "apps/web/app/(main)/jobs/page.tsx"
      provides: "Job list page with server-side data fetching"
      exports: ["default"]
    - path: "apps/web/components/jobs/job-list-table.tsx"
      provides: "Table component for job listings"
      exports: ["JobListTable"]
    - path: "apps/web/components/jobs/job-list-filters.tsx"
      provides: "Nationality and sort filter component"
      exports: ["JobListFilters"]
    - path: "apps/web/components/jobs/login-modal.tsx"
      provides: "Login prompt modal for unauthenticated detail access"
      exports: ["LoginModal"]
  key_links:
    - from: "apps/web/app/(main)/jobs/page.tsx"
      to: "supabase job_posts table"
      via: "createClient() query with filter and pagination"
      pattern: "from\\('job_posts'\\)"
    - from: "apps/web/components/jobs/job-list-filters.tsx"
      to: "URL searchParams"
      via: "router.push with updated params"
      pattern: "router\\.push"
---

<objective>
Create the job list page with nationality filtering, pagination, and login modal.

Purpose: Enables non-authenticated users to browse published job posts and see the list without login. Clicking a job detail requires authentication (handled via modal).
Output: Working /jobs page with table, filters, pagination, and login modal for detail access.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-seeker-experience/03-CONTEXT.md
@.planning/phases/03-job-seeker-experience/03-RESEARCH.md
@apps/web/middleware.ts
@packages/lib/src/constants/nationalities.ts
@supabase/migrations/00001_create_base_schema.sql
@supabase/migrations/00002_create_rls_policies.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RPC functions and install shadcn components</name>
  <files>
    supabase/migrations/00004_rpc_functions.sql
    apps/web/components/ui/table.tsx
    apps/web/components/ui/badge.tsx
    apps/web/components/ui/dialog.tsx
    apps/web/components/ui/pagination.tsx
    apps/web/components/ui/skeleton.tsx
  </files>
  <action>
    1. Create migration file `supabase/migrations/00004_rpc_functions.sql`:
       - `increment_view_count(post_id UUID)` - SECURITY DEFINER function that increments view_count for published posts. Grant to authenticated only.
       - `get_like_count(post_id UUID)` - SECURITY DEFINER function that returns count of likes for a post. Grant to anon and authenticated.
       - `user_liked_post(post_id UUID)` - SECURITY DEFINER function that returns boolean if current user liked the post. Grant to authenticated.
       - Add RLS policy for global_metrics_config: "Anyone can read metrics config" to anon and authenticated.

    2. Install shadcn/ui components in apps/web:
       ```bash
       cd apps/web && pnpm dlx shadcn@latest add table badge dialog pagination skeleton
       ```
       Use default options for any prompts.

    3. Run the migration against Supabase:
       ```bash
       cd /Users/2303-pc02/potenlab/hire_foreigner && npx supabase db push
       ```
       If Supabase CLI not authenticated, this is expected - migration file is ready for manual push.
  </action>
  <verify>
    - Migration file exists at supabase/migrations/00004_rpc_functions.sql
    - shadcn/ui components exist: apps/web/components/ui/{table,badge,dialog,pagination,skeleton}.tsx
    - `pnpm build` passes from workspace root
  </verify>
  <done>RPC functions defined in migration, shadcn/ui table components installed</done>
</task>

<task type="auto">
  <name>Task 2: Create job list page with filters and pagination</name>
  <files>
    apps/web/middleware.ts
    apps/web/app/(main)/layout.tsx
    apps/web/app/(main)/jobs/page.tsx
    apps/web/components/jobs/job-list-table.tsx
    apps/web/components/jobs/job-list-filters.tsx
    apps/web/components/jobs/job-list-pagination.tsx
    apps/web/components/jobs/login-modal.tsx
  </files>
  <action>
    1. Update middleware.ts:
       - Ensure `/jobs` is in publicRoutes (already there per existing file)
       - Add logic: if pathname starts with `/jobs/` and is not exactly `/jobs`, redirect unauthenticated users to login OR let it pass (detail page handles modal)
       - For simplicity: allow /jobs/* to pass, let the detail page handle auth via modal

    2. Create (main) route group layout at `apps/web/app/(main)/layout.tsx`:
       - Simple layout wrapper for main app pages
       - Import and display children
       - Can add header/nav later if needed

    3. Create job list page at `apps/web/app/(main)/jobs/page.tsx`:
       - Server component with searchParams: { nationality?: string, page?: string, sort?: string }
       - Await searchParams (Next.js 15 pattern)
       - Parse page (default 1), pageSize (10), sortBy (default 'latest')
       - Query job_posts with:
         - `.eq('review_status', 'published')`
         - If nationality set and not 'all': `.or(\`target_nationality.eq.${nationality},target_nationality.eq.ANY\`)`
         - Sort: 'popular' uses view_count desc, 'latest' uses published_at desc
         - Pagination: `.range((page-1)*10, page*10-1)`
         - Select with `{ count: 'exact' }`
       - Fetch global_metrics_config for ramp_days and curve_strength
       - Pass posts, count, config, isAuthenticated to child components
       - Calculate display metrics (real + fake) for each post using utility function

    4. Create `apps/web/lib/utils/metrics.ts`:
       - `calculateFakeMetric(target, publishedAt, config)` - returns fake metric using log curve
       - `getDisplayMetrics(realViews, realLikes, viewTarget, likeTarget, publishedAt, config)` - returns combined metrics

    5. Create `apps/web/components/jobs/job-list-filters.tsx`:
       - 'use client'
       - Two Select dropdowns: nationality (from NATIONALITIES), sort (latest/popular)
       - On change: update URL searchParams, reset page to 1
       - Use router.push with updated params

    6. Create `apps/web/components/jobs/job-list-table.tsx`:
       - Server component receiving posts array, isAuthenticated flag, metricsConfig
       - Render Table with columns: 날짜 (published_at), 제목 (title + Badge for hiring_status), 조회수, 관심수
       - Each row is a JobRow client component that handles click

    7. Create `apps/web/components/jobs/job-row.tsx`:
       - 'use client' component
       - Props: job, isAuthenticated, displayViews, displayLikes
       - On click: if authenticated, router.push to /jobs/[id]; else open LoginModal
       - Render TableRow with TableCells

    8. Create `apps/web/components/jobs/job-list-pagination.tsx`:
       - 'use client'
       - Receive currentPage, totalPages
       - Use shadcn Pagination components
       - Create page URLs preserving existing searchParams

    9. Create `apps/web/components/jobs/login-modal.tsx`:
       - 'use client'
       - Dialog with message: "로그인이 필요합니다" and "공고 상세를 보려면 로그인해 주세요"
       - Button to navigate to /login
  </action>
  <verify>
    - `pnpm build` passes
    - Navigate to http://localhost:3000/jobs shows job list (may be empty if no published posts)
    - Changing nationality filter updates URL
    - Pagination controls appear if > 10 posts
    - Clicking a row when logged out shows login modal
  </verify>
  <done>Job list page displays published posts with filtering, pagination, and login modal for unauthenticated detail clicks</done>
</task>

</tasks>

<verification>
1. Run `pnpm build` from workspace root - must pass
2. Start dev server: `pnpm dev`
3. Navigate to http://localhost:3000/jobs - page loads without auth
4. Verify nationality filter dropdown contains 15 countries + "전체"
5. Verify changing filter updates URL (?nationality=VN)
6. Verify pagination appears if > 10 published posts
7. If logged out, click a job row - login modal appears
8. If logged in, click a job row - navigates to /jobs/[id] (will 404 until Plan 02)
</verification>

<success_criteria>
- Migration file with RPC functions exists and is syntactically correct
- /jobs page renders for both authenticated and unauthenticated users
- Filtering by nationality updates URL and filters posts (including ANY nationality posts)
- Pagination works correctly with URL state preservation
- Login modal appears when unauthenticated user clicks a job row
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-seeker-experience/03-01-SUMMARY.md`
</output>
