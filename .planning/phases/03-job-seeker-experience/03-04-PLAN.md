---
phase: 03-job-seeker-experience
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/app/(main)/my-page/page.tsx
  - apps/web/app/actions/profile.ts
  - apps/web/lib/validations/profile.ts
  - apps/web/components/my-page/profile-tab.tsx
  - apps/web/components/my-page/liked-jobs-tab.tsx
  - apps/web/components/my-page/profile-edit-modal.tsx
  - apps/web/components/ui/tabs.tsx
autonomous: true

must_haves:
  truths:
    - "Seekers can access my page at /my-page"
    - "My page shows two tabs: profile and liked jobs"
    - "Profile tab displays current nationality, TOPIK level, occupation, referral source"
    - "Profile edit modal allows updating nationality, TOPIK, occupation, referral source"
    - "Liked jobs tab shows list of jobs the seeker has liked"
    - "Liked jobs list shows status, title, date, view count"
  artifacts:
    - path: "apps/web/app/(main)/my-page/page.tsx"
      provides: "My page with tabs for profile and liked jobs"
      exports: ["default"]
    - path: "apps/web/app/actions/profile.ts"
      provides: "Server action for updating seeker profile"
      exports: ["updateSeekerProfile"]
    - path: "apps/web/components/my-page/profile-tab.tsx"
      provides: "Profile display with edit button"
      exports: ["ProfileTab"]
    - path: "apps/web/components/my-page/liked-jobs-tab.tsx"
      provides: "Liked jobs list"
      exports: ["LikedJobsTab"]
    - path: "apps/web/components/my-page/profile-edit-modal.tsx"
      provides: "Profile edit form in modal"
      exports: ["ProfileEditModal"]
  key_links:
    - from: "apps/web/components/my-page/profile-edit-modal.tsx"
      to: "apps/web/app/actions/profile.ts"
      via: "form action calling updateSeekerProfile"
      pattern: "updateSeekerProfile"
    - from: "apps/web/app/(main)/my-page/page.tsx"
      to: "supabase seeker_profiles and likes tables"
      via: "createClient() queries"
      pattern: "from\\('seeker_profiles'\\)|from\\('likes'\\)"
---

<objective>
Create the seeker my page with profile viewing/editing and liked jobs list.

Purpose: Seekers can manage their profile and view jobs they've liked. Profile editing uses a modal per CONTEXT.md decision.
Output: Working /my-page with two tabs (profile, liked jobs) and profile edit modal.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-job-seeker-experience/03-CONTEXT.md
@.planning/phases/03-job-seeker-experience/03-RESEARCH.md
@.planning/phases/03-job-seeker-experience/03-01-SUMMARY.md
@apps/web/lib/validations/auth.ts
@apps/web/components/auth/seeker-form.tsx
@packages/lib/src/constants/nationalities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile validation schema and update action</name>
  <files>
    apps/web/lib/validations/profile.ts
    apps/web/app/actions/profile.ts
  </files>
  <action>
    1. Create `apps/web/lib/validations/profile.ts`:
       - Import z from zod
       - Import NATIONALITIES from @repo/lib

       - Create seekerProfileUpdateSchema:
         - nationality: z.enum() with NATIONALITIES codes excluding 'ANY'
         - topik_level: z.number().min(0).max(6).nullable().optional()
         - occupation: z.string().max(100).nullable().optional()
         - referral_source: z.string().max(200).nullable().optional()

       - Export the schema and infer the type

    2. Create `apps/web/app/actions/profile.ts`:
       - 'use server' directive
       - Import createClient from @repo/supabase/server
       - Import revalidatePath from next/cache
       - Import seekerProfileUpdateSchema from lib/validations/profile

       - Export async function updateSeekerProfile(formData: FormData):
         a. Create Supabase client
         b. Get user with getUser() - throw if not authenticated
         c. Parse formData into raw object:
            - nationality: formData.get('nationality')
            - topik_level: parse as number or null
            - occupation: formData.get('occupation') or null
            - referral_source: formData.get('referral_source') or null
         d. Validate with seekerProfileUpdateSchema.safeParse()
         e. If validation fails, return { error: result.error.flatten().fieldErrors }
         f. Update seeker_profiles where user_id = user.id
            - Use 'as any' for TypeScript workaround
         g. revalidatePath('/my-page')
         h. Return { success: true }
  </action>
  <verify>
    - `pnpm build` passes
    - Validation schema correctly validates nationality (no ANY), optional TOPIK/occupation/referral
  </verify>
  <done>Profile validation schema and update server action ready for use</done>
</task>

<task type="auto">
  <name>Task 2: Create my page with tabs and components</name>
  <files>
    apps/web/components/ui/tabs.tsx
    apps/web/app/(main)/my-page/page.tsx
    apps/web/components/my-page/profile-tab.tsx
    apps/web/components/my-page/liked-jobs-tab.tsx
    apps/web/components/my-page/profile-edit-modal.tsx
  </files>
  <action>
    1. Install shadcn/ui tabs:
       ```bash
       cd apps/web && pnpm dlx shadcn@latest add tabs
       ```

    2. Create `apps/web/app/(main)/my-page/page.tsx`:
       - Server component
       - Get user with getUser() - redirect to /login if not authenticated
       - Fetch seeker_profiles where user_id = user.id
       - If no seeker profile, redirect to '/' (not a seeker, shouldn't access my-page)
       - Fetch liked jobs using joined query:
         ```typescript
         const { data: likedJobs } = await supabase
           .from('likes')
           .select(`
             id,
             post:job_posts(
               id, title, hiring_status, published_at, view_count, view_target, like_target
             )
           `)
           .eq('user_id', user.id)
           .order('created_at', { ascending: false })
         ```
       - Fetch global_metrics_config for display calculations
       - Render:
         - h1: "마이페이지"
         - Tabs with two TabsTrigger: "프로필", "관심 공고"
         - TabsContent for profile: ProfileTab with profile data
         - TabsContent for liked: LikedJobsTab with likedJobs and metricsConfig

    3. Create `apps/web/components/my-page/profile-tab.tsx`:
       - 'use client' (needs state for modal)
       - Props: profile object (nationality, topik_level, occupation, referral_source)
       - State: isEditModalOpen
       - Display profile info in a Card:
         - 국적: get Korean name from NATIONALITIES
         - TOPIK: 0-6 or "미입력"
         - 직업: occupation or "미입력"
         - 유입 경로: referral_source or "미입력"
       - "수정" button opens ProfileEditModal
       - Render ProfileEditModal with defaultValues={profile}

    4. Create `apps/web/components/my-page/profile-edit-modal.tsx`:
       - 'use client'
       - Props: open, onOpenChange, defaultValues (profile data)
       - Use Dialog from shadcn/ui
       - Form with:
         - Nationality Select (from NATIONALITIES, excluding ANY)
         - TOPIK level Select (0-6 options)
         - Occupation Input (optional)
         - Referral source Input (optional)
       - Use react-hook-form with zodResolver and seekerProfileUpdateSchema
       - On submit: call updateSeekerProfile server action with FormData
       - Show loading state, close modal on success
       - Handle errors (display field errors)

    5. Create `apps/web/components/my-page/liked-jobs-tab.tsx`:
       - Server component (or client if needed for interaction)
       - Props: jobs array, metricsConfig
       - If empty, show message: "관심 표시한 공고가 없습니다"
       - Otherwise, render Table (reuse similar structure to job-list-table):
         - Columns: 날짜, 제목 (with hiring status badge), 조회수
         - Each row links to /jobs/[id]
       - Calculate display metrics for each job
  </action>
  <verify>
    - `pnpm build` passes
    - Navigate to /my-page as seeker - shows tabs
    - Profile tab shows current profile data
    - Click "수정" - modal opens with pre-filled data
    - Update a field, submit - profile updates, modal closes
    - Liked jobs tab shows jobs user has liked (or empty message)
    - Clicking a liked job navigates to detail page
  </verify>
  <done>My page with profile viewing/editing and liked jobs list fully functional</done>
</task>

</tasks>

<verification>
1. Run `pnpm build` from workspace root - must pass
2. Start dev server: `pnpm dev`
3. Log in as seeker
4. Navigate to /my-page - two tabs visible
5. Profile tab shows current nationality, TOPIK, occupation, referral source
6. Click "수정" button - modal opens with current values pre-filled
7. Change a value (e.g., occupation), submit - modal closes, value updated
8. Navigate to "관심 공고" tab - shows liked jobs or empty state
9. If jobs liked, verify they display with status, title, date, views
10. Click a liked job - navigates to /jobs/[id]
11. Log in as employer, navigate to /my-page - should redirect to / (employers don't have my-page)
</verification>

<success_criteria>
- My page accessible only to seekers
- Profile tab displays all profile fields correctly
- Profile edit modal allows updating all fields with validation
- Liked jobs tab shows all liked jobs with correct data
- Empty state handled gracefully
- Navigation from liked jobs to detail works
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-job-seeker-experience/03-04-SUMMARY.md`
</output>
