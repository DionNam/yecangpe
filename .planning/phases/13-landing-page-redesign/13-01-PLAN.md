---
phase: 13-landing-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/landing/hero-section.tsx
  - apps/web/components/landing/social-proof-section.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section displays 'Find Korean-Speaking Jobs Worldwide' headline"
    - "Hero section has dual CTA buttons: 'I'm a Job Seeker' linking to /job-seekers and 'I'm an Employer' linking to /employers"
    - "Social Proof section displays 3 animated counters for jobs, companies, and members"
    - "Animated counters accept numeric props and animate on scroll into view"
  artifacts:
    - path: "apps/web/components/landing/hero-section.tsx"
      provides: "Dual CTA hero section with HangulJobs branding"
      contains: "I'm a Job Seeker"
    - path: "apps/web/components/landing/social-proof-section.tsx"
      provides: "Social proof with animated counters"
      contains: "AnimatedCounter"
  key_links:
    - from: "apps/web/components/landing/hero-section.tsx"
      to: "/job-seekers"
      via: "Next.js Link component"
      pattern: "href.*job-seekers"
    - from: "apps/web/components/landing/social-proof-section.tsx"
      to: "apps/web/components/landing/animated-counter.tsx"
      via: "import AnimatedCounter"
      pattern: "import.*AnimatedCounter"
---

<objective>
Replace the existing hero section with a new dual-CTA hero and create a new social proof section with animated counters.

Purpose: The hero is the first thing users see. Dual CTAs serve both job seekers and employers. Social proof with animated counters builds trust immediately.
Output: Two new landing page sections (hero-section.tsx replaced, social-proof-section.tsx created)
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@apps/web/components/landing/hero-section.tsx (current hero to replace)
@apps/web/components/landing/animated-counter.tsx (reuse for social proof)
@.planning/phases/13-landing-page-redesign/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Hero Section with Dual CTA</name>
  <files>apps/web/components/landing/hero-section.tsx</files>
  <action>
Replace the entire hero-section.tsx with a new design:

1. Keep 'use client' directive and Motion animations
2. Remove the HeroSectionProps interface (no longer needs memberCount prop)
3. New layout structure:
   - Full-width section with gradient background (from-slate-50 via-white to-blue-50)
   - Centered content (not 2-column grid) for maximum impact
   - Badge/chip: "전 세계 한국어 일자리 플랫폼" (bilingual tagline from branding)
   - Main headline: "Find Korean-Speaking Jobs Worldwide" (large, bold, text-4xl md:text-5xl lg:text-6xl)
   - Subtitle in Korean: "전 세계 한국어 일자리를 한곳에서" (smaller, text-gray-600)
   - Dual CTA buttons:
     - Primary (filled blue-600): "I'm a Job Seeker" with ArrowRight icon -> links to /job-seekers
     - Secondary (outlined, white bg with blue-600 border): "I'm an Employer" with Briefcase icon -> links to /employers
   - Three trust indicators below CTAs (same pattern as current hero):
     - "Verified Job Posts" (checkmark icon)
     - "Global Opportunities" (globe icon)
     - "Korean Language Focus" (languages icon)
4. Use Motion staggered animations (opacity 0->1, y 20->0) with increasing delays (0, 0.1, 0.2, 0.3)
5. Import icons from lucide-react: ArrowRight, Briefcase, CheckCircle, Globe, Languages
6. Responsive: stack CTAs vertically on mobile (flex-col sm:flex-row)
7. Remove the hero image (Image import and right column) - clean text-focused hero
8. Use Pretendard font classes (font-sans is already mapped to Pretendard)

Export: `export function HeroSection()` (no props needed)
  </action>
  <verify>
Check file exists and has correct structure:
- grep "I'm a Job Seeker" apps/web/components/landing/hero-section.tsx
- grep "I'm an Employer" apps/web/components/landing/hero-section.tsx
- grep "/job-seekers" apps/web/components/landing/hero-section.tsx
- grep "/employers" apps/web/components/landing/hero-section.tsx
- grep "Find Korean-Speaking Jobs Worldwide" apps/web/components/landing/hero-section.tsx
  </verify>
  <done>Hero section displays bilingual headline with dual CTA buttons linking to /job-seekers and /employers. No props required.</done>
</task>

<task type="auto">
  <name>Task 2: Create Social Proof Section</name>
  <files>apps/web/components/landing/social-proof-section.tsx</files>
  <action>
Create a new social-proof-section.tsx component:

1. 'use client' directive (uses AnimatedCounter which is client component)
2. Interface: SocialProofSectionProps { jobCount: number; companyCount: number; memberCount: number }
3. Import AnimatedCounter from './animated-counter'
4. Layout:
   - Section with py-16 bg-white
   - max-w-7xl mx-auto px-6
   - Grid: md:grid-cols-3 gap-8, centered text
   - Three counter cards:
     a. Jobs: AnimatedCounter value={jobCount} suffix="+" className="text-5xl font-bold text-blue-600"
        Label: "Active Jobs" / "등록 채용 공고"
     b. Companies: AnimatedCounter value={companyCount} suffix="+" className="text-5xl font-bold text-amber-600"
        Label: "Companies" / "등록 기업"
     c. Members: AnimatedCounter value={memberCount} suffix="+" className="text-5xl font-bold text-emerald-600"
        Label: "Job Seekers" / "가입 구직자"
   - Each card: text-center with icon above counter (Briefcase for jobs, Building2 for companies, Users for members)
   - Icons: w-8 h-8 mx-auto mb-3 text-gray-400
5. Wrap section in motion.div with whileInView animation for fade-in effect
6. Use viewport={{ once: true }} to prevent re-triggering

Export: `export function SocialProofSection({ jobCount, companyCount, memberCount }: SocialProofSectionProps)`

IMPORTANT: This section receives pre-calculated counts from page.tsx (including offsets). It does NOT fetch data itself.
  </action>
  <verify>
Check file exists and imports AnimatedCounter:
- grep "AnimatedCounter" apps/web/components/landing/social-proof-section.tsx
- grep "jobCount" apps/web/components/landing/social-proof-section.tsx
- grep "companyCount" apps/web/components/landing/social-proof-section.tsx
- grep "memberCount" apps/web/components/landing/social-proof-section.tsx
  </verify>
  <done>Social proof section renders 3 animated counters (jobs, companies, members) with bilingual labels and color-coded values matching the brand palette.</done>
</task>

</tasks>

<verification>
1. hero-section.tsx exports HeroSection with no props
2. social-proof-section.tsx exports SocialProofSection with jobCount, companyCount, memberCount props
3. Both files use 'use client' directive
4. Hero has dual CTA links to /job-seekers and /employers
5. Social proof imports and uses AnimatedCounter from ./animated-counter
6. No TypeScript compilation errors
</verification>

<success_criteria>
- Hero section displays "Find Korean-Speaking Jobs Worldwide" headline
- Dual CTA buttons render: "I'm a Job Seeker" (primary) and "I'm an Employer" (secondary)
- Social proof section displays 3 animated counters with proper color coding
- Both sections are responsive (mobile-first)
</success_criteria>

<output>
After completion, create `.planning/phases/13-landing-page-redesign/13-01-SUMMARY.md`
</output>
