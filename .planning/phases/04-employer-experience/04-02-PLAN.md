---
phase: 04-employer-experience
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apps/web/app/actions/jobs.ts
  - apps/web/app/(main)/employer/posts/page.tsx
  - apps/web/components/employer/my-posts-table.tsx
  - apps/web/components/employer/post-edit-modal.tsx
  - apps/web/components/employer/hiring-status-toggle.tsx
autonomous: true

must_haves:
  truths:
    - "Employer can view list of their posts with status, hiring status, title, views, likes, date"
    - "Employer can edit title and content of pending or published posts"
    - "Employer can toggle hiring status (hiring/closed) for published posts only"
    - "Employer can see rejection reason for rejected posts"
  artifacts:
    - path: "apps/web/app/(main)/employer/posts/page.tsx"
      provides: "My posts management page for employers"
      min_lines: 50
    - path: "apps/web/components/employer/my-posts-table.tsx"
      provides: "Table component for employer's posts"
      min_lines: 80
    - path: "apps/web/components/employer/post-edit-modal.tsx"
      provides: "Modal for editing post title and content"
      min_lines: 60
    - path: "apps/web/components/employer/hiring-status-toggle.tsx"
      provides: "Toggle for hiring/closed status"
      min_lines: 25
    - path: "apps/web/app/actions/jobs.ts"
      provides: "Server actions for updating posts"
      exports: ["createJobPost", "updateJobPost", "updateHiringStatus"]
  key_links:
    - from: "apps/web/components/employer/post-edit-modal.tsx"
      to: "apps/web/app/actions/jobs.ts"
      via: "updateJobPost server action call"
      pattern: "updateJobPost"
    - from: "apps/web/components/employer/hiring-status-toggle.tsx"
      to: "apps/web/app/actions/jobs.ts"
      via: "updateHiringStatus server action call"
      pattern: "updateHiringStatus"
    - from: "apps/web/app/(main)/employer/posts/page.tsx"
      to: "apps/web/components/employer/my-posts-table.tsx"
      via: "MyPostsTable component import"
      pattern: "MyPostsTable"
---

<objective>
Create my posts management page for employers with table view, edit modal, hiring status toggle, and rejection reason display.

Purpose: Enable employers to manage their job postings - view status, edit content, change hiring status, see rejection reasons (EMPM-01, EMPM-02, EMPM-03, EMPM-04).
Output: My posts page with interactive table, edit functionality, and status management.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-employer-experience/04-RESEARCH.md
@.planning/phases/04-employer-experience/04-01-SUMMARY.md

# Existing patterns from seeker my-page
@apps/web/app/(main)/my-page/page.tsx
@apps/web/components/my-page/profile-edit-modal.tsx
@apps/web/components/my-page/liked-jobs-tab.tsx

# UI components
@apps/web/components/ui/table.tsx
@apps/web/components/ui/badge.tsx
@apps/web/components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update server actions to jobs.ts</name>
  <files>
    apps/web/app/actions/jobs.ts
  </files>
  <action>
    Add two new server actions to the existing jobs.ts file (from 04-01):

    1. updateJobPost server action:
       - Accepts FormData with id, title, content
       - Verify authentication (getUser)
       - Verify ownership (author_id matches user.id)
       - Validate with jobPostUpdateSchema (title and content only, per EMPM-02)
       - Update job_posts record
       - revalidatePath('/employer/posts')
       - Return { success: true } or { error: fieldErrors }

    2. updateHiringStatus server action:
       - Accepts postId: string and newStatus: 'hiring' | 'closed'
       - Verify authentication
       - Verify ownership (author_id matches user.id)
       - Verify post is published (review_status === 'published') - only published posts can change hiring status
       - Update hiring_status field
       - revalidatePath('/employer/posts')
       - Return { success: true }
       - Throw error if not owner or not published

    Both actions use established 'as any' pattern for Supabase operations.
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Check that jobs.ts exports createJobPost, updateJobPost, updateHiringStatus.
  </verify>
  <done>
    Server actions for updating job post content and hiring status work correctly with ownership verification.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create my-posts-table, edit modal, and hiring toggle components</name>
  <files>
    apps/web/components/employer/my-posts-table.tsx
    apps/web/components/employer/post-edit-modal.tsx
    apps/web/components/employer/hiring-status-toggle.tsx
  </files>
  <action>
    1. Create hiring status toggle component:
       - File: apps/web/components/employer/hiring-status-toggle.tsx
       - 'use client' component
       - Props: postId, currentStatus ('hiring' | 'closed'), disabled (for non-published)
       - useTransition for pending state
       - Button that calls updateHiringStatus on click
       - Display "채용중" or "마감" based on status
       - Disabled state for pending/rejected posts

    2. Create post edit modal:
       - File: apps/web/components/employer/post-edit-modal.tsx
       - 'use client' component
       - Follow profile-edit-modal.tsx pattern
       - Props: open, onOpenChange, postId, defaultValues (title, content)
       - useForm with zodResolver(jobPostUpdateSchema)
       - Form fields: title (Input), content (Textarea)
       - Submit calls updateJobPost with FormData
       - Close on success, show errors on failure

    3. Create my posts table component:
       - File: apps/web/components/employer/my-posts-table.tsx
       - 'use client' component
       - Props: posts array, metricsConfig (ramp_days, curve_strength), likeCounts record
       - State: editingPost (for modal), isEditOpen
       - Table columns:
         * 상태: Badge for review_status (pending=심사중 yellow, published=게시됨 green, rejected=반려 red)
         * 채용상태: HiringStatusToggle (only enabled for published)
         * 제목: post.title (clickable link to /jobs/{id} for published)
         * 조회수: Display calculated metrics (only for published, "-" for others)
         * 관심수: Display calculated metrics (only for published, "-" for others)
         * 작성일: formatted date
         * 액션: Edit button (opens modal), rejection reason (for rejected)
       - For rejected posts, show rejection_reason in Alert or inline text
       - Calculate display metrics using same logic as seeker my-page (log curve)
       - Empty state message when no posts
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Verify all three component files exist with correct exports.
  </verify>
  <done>
    Table displays posts with correct status badges, edit modal updates content, hiring toggle changes status for published posts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create employer posts management page</name>
  <files>
    apps/web/app/(main)/employer/posts/page.tsx
  </files>
  <action>
    Create the my posts page:
    - File: apps/web/app/(main)/employer/posts/page.tsx
    - Server component
    - Check authentication, redirect to /login if not authenticated
    - Check employer role (employer_profiles query), redirect to / if not employer
    - Fetch employer's posts ordered by created_at descending:
      * Select all fields including rejection_reason
      * Filter by author_id = user.id
    - Fetch global_metrics_config (ramp_days, curve_strength) for display calculations
    - Fetch like counts for each post using get_like_count RPC (or count query)
    - Page layout:
      * Container with py-8
      * Header with "내 공고 관리" title
      * "구인글 올리기" button linking to /employer/new-post
      * MyPostsTable component with posts, metricsConfig, likeCounts props
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Verify page file exists at correct path.
    Manually test by visiting /employer/posts as an employer user.
  </verify>
  <done>
    My posts page loads for employers, displays all their posts with management capabilities, non-employers are redirected.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `pnpm build` completes without errors
2. Posts list displays: All employer's posts shown with correct status badges
3. Edit works: Clicking edit opens modal, saving updates title/content
4. Hiring toggle works: Published posts can toggle between hiring/closed
5. Rejection reason shows: Rejected posts display rejection_reason
6. Metrics display correctly: Published posts show calculated view/like counts
7. Navigation works: "구인글 올리기" button navigates to new-post page
</verification>

<success_criteria>
- Employer can view list of their posts at /employer/posts
- Each post shows review status badge (심사중/게시됨/반려)
- Hiring status toggle only works for published posts
- Edit modal allows updating title and content
- Rejected posts show rejection reason
- Published posts show calculated view and like counts
- Pending posts show "-" for metrics (not calculated until published)
- "구인글 올리기" button links to /employer/new-post
- Non-employers and unauthenticated users cannot access the page
</success_criteria>

<output>
After completion, create `.planning/phases/04-employer-experience/04-02-SUMMARY.md`
</output>
