---
phase: 04-employer-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/ui/textarea.tsx
  - apps/web/lib/validations/job-post.ts
  - apps/web/app/actions/jobs.ts
  - apps/web/components/employer/job-post-form.tsx
  - apps/web/components/employer/submission-dialog.tsx
  - apps/web/app/(main)/employer/new-post/page.tsx
autonomous: true

must_haves:
  truths:
    - "Employer can access job posting form from navigation"
    - "Form validates title (1-100 chars), content (10-5000 chars), company name (1-100 chars), nationality (required)"
    - "Submission creates job post with pending review status"
    - "Confirmation dialog shows approval notice and redirects to my posts"
  artifacts:
    - path: "apps/web/components/ui/textarea.tsx"
      provides: "Multi-line text input component"
      min_lines: 15
    - path: "apps/web/lib/validations/job-post.ts"
      provides: "Zod schemas for job post creation and update"
      exports: ["jobPostSchema", "jobPostUpdateSchema", "JobPostInput", "JobPostUpdateInput"]
    - path: "apps/web/app/actions/jobs.ts"
      provides: "Server action for creating job posts"
      exports: ["createJobPost"]
    - path: "apps/web/components/employer/job-post-form.tsx"
      provides: "Job posting form with validation"
      min_lines: 80
    - path: "apps/web/components/employer/submission-dialog.tsx"
      provides: "Submission confirmation dialog"
      min_lines: 25
    - path: "apps/web/app/(main)/employer/new-post/page.tsx"
      provides: "Job posting page for employers"
      min_lines: 30
  key_links:
    - from: "apps/web/components/employer/job-post-form.tsx"
      to: "apps/web/app/actions/jobs.ts"
      via: "createJobPost server action call"
      pattern: "createJobPost"
    - from: "apps/web/app/(main)/employer/new-post/page.tsx"
      to: "apps/web/components/employer/job-post-form.tsx"
      via: "JobPostForm component import"
      pattern: "JobPostForm"
---

<objective>
Create job posting form for employers with validation, server action, and submission confirmation dialog.

Purpose: Enable employers to create job postings that enter the pending review queue (EMPL-01, EMPL-02, EMPL-03, EMPL-04).
Output: New-post page with form, validation schema, server action, and confirmation dialog.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-employer-experience/04-RESEARCH.md

# Existing patterns
@apps/web/components/auth/employer-form.tsx
@apps/web/app/actions/auth.ts
@apps/web/components/ui/form.tsx
@apps/web/components/ui/dialog.tsx
@packages/lib/src/constants/nationalities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Textarea component and job post validation schema</name>
  <files>
    apps/web/components/ui/textarea.tsx
    apps/web/lib/validations/job-post.ts
  </files>
  <action>
    1. Create Textarea component following shadcn/ui pattern:
       - File: apps/web/components/ui/textarea.tsx
       - Standard shadcn Textarea with cn utility, ring focus states
       - min-h-[80px] default, accepts className override
       - forwardRef pattern matching existing Input component

    2. Create job post validation schema:
       - File: apps/web/lib/validations/job-post.ts
       - Import NATIONALITIES from @repo/lib for nationality codes
       - jobPostSchema with fields:
         * title: z.string().min(1, '제목을 입력해주세요').max(100, '제목은 100자 이내로 입력해주세요')
         * content: z.string().min(10, '내용을 10자 이상 입력해주세요').max(5000, '내용은 5000자 이내로 입력해주세요')
         * company_name: z.string().min(1, '회사명을 입력해주세요').max(100, '회사명은 100자 이내로 입력해주세요')
         * target_nationality: z.enum(nationalityCodes) with error message
       - jobPostUpdateSchema with title and content only (for EMPM-02)
       - Export types: JobPostInput, JobPostUpdateInput
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Verify both files exist with correct exports.
  </verify>
  <done>
    Textarea component renders correctly, validation schemas type-check and export properly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create job post server action and form component</name>
  <files>
    apps/web/app/actions/jobs.ts
    apps/web/components/employer/job-post-form.tsx
    apps/web/components/employer/submission-dialog.tsx
  </files>
  <action>
    1. Create createJobPost server action:
       - File: apps/web/app/actions/jobs.ts
       - 'use server' directive
       - Verify user authentication (getUser)
       - Verify employer role (check employer_profiles table)
       - Parse FormData and validate with jobPostSchema
       - Fetch global_metrics_config for view_target and like_target ranges
       - Calculate random target values within configured ranges
       - Insert job_posts with:
         * author_id: user.id
         * title, content, company_name, target_nationality from form
         * review_status: 'pending'
         * hiring_status: 'hiring'
         * view_target: random within range
         * like_target: random within range
       - Return { success: true } or { error: fieldErrors }
       - Use 'as any' pattern for Supabase inserts (established pattern)

    2. Create submission confirmation dialog:
       - File: apps/web/components/employer/submission-dialog.tsx
       - 'use client' component
       - Dialog with title "공고가 제출되었습니다"
       - Description "1일 이내 관리자 승인 후 게시됩니다."
       - Single "확인" button that closes dialog and redirects to /employer/posts
       - Follow existing dialog.tsx patterns

    3. Create job posting form component:
       - File: apps/web/components/employer/job-post-form.tsx
       - 'use client' component
       - Props: defaultCompanyName (pre-filled from employer profile)
       - useForm with zodResolver(jobPostSchema)
       - useTransition for pending state
       - useState for showDialog (submission confirmation)
       - Form fields:
         * title: Input
         * content: Textarea with min-h-[300px] and placeholder with example structure
         * company_name: Input (pre-filled)
         * target_nationality: Select with NATIONALITIES from @repo/lib
       - On submit: create FormData, call createJobPost action
       - On success: show SubmissionDialog
       - On error: set field errors via form.setError
       - Submit button with pending state
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Check that jobs.ts exports createJobPost.
    Check that JobPostForm and SubmissionDialog components exist.
  </verify>
  <done>
    Server action validates input and inserts pending job post, form handles submission and shows confirmation dialog.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create new-post page with employer access check</name>
  <files>
    apps/web/app/(main)/employer/new-post/page.tsx
  </files>
  <action>
    1. Create employer directory and new-post page:
       - File: apps/web/app/(main)/employer/new-post/page.tsx
       - Server component (no 'use client')
       - Check authentication (getUser), redirect to /login if not authenticated
       - Check employer role (employer_profiles query), redirect to / if not employer
       - Fetch employer's company_name from employer_profiles to pre-fill form
       - Render page with:
         * Container with py-8
         * Back link to /employer/posts (if exists) or header with "구인글 작성"
         * Card containing JobPostForm with defaultCompanyName prop

    2. Page layout:
       - Title: "구인글 작성" or "새 공고 등록"
       - Description: Brief instruction text
       - JobPostForm component with employer's company name as default
  </action>
  <verify>
    Run `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm build` - no TypeScript errors.
    Verify page file exists at correct path.
    Manually test by visiting /employer/new-post as an employer user.
  </verify>
  <done>
    New-post page loads for employers, shows form pre-filled with company name, non-employers are redirected.
  </done>
</task>

</tasks>

<verification>
1. Build passes: `pnpm build` completes without errors
2. Form validation works: Title, content, company_name, nationality all validate correctly
3. Server action creates pending post: Check database for new record with review_status='pending'
4. Confirmation dialog appears: After successful submission, dialog shows with approval message
5. Redirect works: After clicking "확인", user is redirected to /employer/posts
</verification>

<success_criteria>
- Employer can navigate to /employer/new-post
- Form validates all required fields with Korean error messages
- Successful submission creates job_posts record with pending status
- Random view_target and like_target are set from global config
- Confirmation dialog shows "1일 이내 관리자 승인 후 게시됩니다"
- Non-employers and unauthenticated users cannot access the page
</success_criteria>

<output>
After completion, create `.planning/phases/04-employer-experience/04-01-SUMMARY.md`
</output>
