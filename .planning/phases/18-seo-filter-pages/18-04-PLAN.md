---
phase: 18-seo-filter-pages
plan: 04
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - apps/web/app/(main)/jobs/by-country/[country]/page.tsx
autonomous: true

must_haves:
  truths:
    - "/jobs/by-country/KR renders with filtered South Korea jobs"
    - "All 26 country pages are pre-generated via generateStaticParams"
    - "Country names displayed in both English and Korean"
    - "Each country page has unique metadata with country-specific SEO description"
    - "Invalid country codes return 404"
  artifacts:
    - path: "apps/web/app/(main)/jobs/by-country/[country]/page.tsx"
      provides: "Country filter pages (26 pages)"
      exports: ["generateStaticParams", "generateMetadata", "default"]
      min_lines: 60
  key_links:
    - from: "apps/web/app/(main)/jobs/by-country/[country]/page.tsx"
      to: "apps/web/components/filter-pages/filter-page-layout.tsx"
      via: "FilterPageLayout composition"
      pattern: "FilterPageLayout"
    - from: "apps/web/app/(main)/jobs/by-country/[country]/page.tsx"
      to: "@repo/lib constants COUNTRIES"
      via: "country validation and name lookup"
      pattern: "COUNTRIES"
---

<objective>
Create the by-country filter page route with ISR, static generation, metadata, and structured data.

Purpose: Country pages are separate because the COUNTRIES constant has a different structure (code/name/nameEn instead of code/name/nameKo), requiring slightly different lookups. This generates 26 country-specific SEO pages.

Output: 1 route page.tsx file generating 26 SEO-optimized country filter pages.
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-seo-filter-pages/18-RESEARCH.md
@.planning/phases/18-seo-filter-pages/18-01-SUMMARY.md

# Pattern reference
@apps/web/app/(main)/jobs/by-type/[type]/page.tsx
# Country constants (note: name = Korean, nameEn = English - different from other constants)
@packages/lib/src/constants/countries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create by-country filter page route</name>
  <files>apps/web/app/(main)/jobs/by-country/[country]/page.tsx</files>
  <action>
Create the country filter page route. Follows the established pattern but handles the COUNTRIES constant's different field names.

**Key differences from by-type:**
- Param name: `country`
- generateStaticParams: `COUNTRIES.map(({ code }) => ({ country: code }))` - import COUNTRIES from '@repo/lib'
- Query filter: `.eq('work_location_country', country)` - this is the column name for country
- Dimension: `'by-country'`
- **Country constant has `name` (Korean) and `nameEn` (English)** instead of `name` (English) and `nameKo` (Korean). The getFilterPageTitle/data helpers in filter-page-data.ts should already handle this mapping, but be aware when looking up country names directly from the COUNTRIES constant.
- Validate: `COUNTRIES.find(c => c.code === country)` - if not found, notFound()
- Type params as `Promise<{ country: string }>`

**Structure:**
```
export const revalidate = 300
export async function generateStaticParams()  // 26 countries
export async function generateMetadata({ params })  // uses generateFilterPageMetadata('by-country', country)
export default async function JobsByCountryPage({ params })
```

**Page component specifics:**
- Same Supabase query pattern, but filter on `work_location_country` column
- For jobs that have `work_location_country = null` (remote jobs without specific country), those won't appear in country filter pages - this is correct behavior
- Country name display: use COUNTRIES.find(c => c.code === country) to get both `nameEn` and `name` (Korean)
- The title from getFilterPageTitle should already return something like: `{ title: 'Jobs in South Korea', titleKo: '대한민국 채용 공고' }`
- ItemList JSON-LD follows same pattern
- FilterPageLayout composition with all sections

**The company count for country pages** can use the same approximation as other filter pages (just use the job count or count unique company_names from the returned jobs array).
  </action>
  <verify>
File exists and TypeScript compiles: `ls apps/web/app/\(main\)/jobs/by-country/\[country\]/page.tsx && cd apps/web && npx tsc --noEmit --pretty 2>&1 | head -20`
  </verify>
  <done>
/jobs/by-country/[country] route exists with generateStaticParams (26 countries), country-specific SEO metadata with bilingual names, ISR, FilterPageLayout, ItemList JSON-LD, notFound() for invalid country codes.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx tsc --noEmit` - TypeScript compiles
2. Directory: `ls apps/web/app/\(main\)/jobs/by-country/\[country\]/page.tsx`
3. Grep for COUNTRIES confirms constant usage
4. Grep for work_location_country confirms correct DB column
5. Grep for generateStaticParams confirms 26 pages will be generated
</verification>

<success_criteria>
- /jobs/by-country/[country] generates 26 pages (all COUNTRIES codes: KR, JP, CN, etc.)
- Each page shows country name in both English and Korean
- Each page has unique meta title like "Jobs in South Korea | HangulJobs"
- Canonical URL set to /jobs/by-country/{code}
- Invalid country codes return 404
- ISR revalidation at 300 seconds
- FilterPageLayout with all sections on each page
</success_criteria>

<output>
After completion, create `.planning/phases/18-seo-filter-pages/18-04-SUMMARY.md`
</output>
