---
phase: 06-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
  - apps/web/components/landing/hero-section.tsx
  - apps/web/components/landing/why-employers-section.tsx
  - apps/web/components/landing/why-talent-section.tsx
  - apps/web/components/landing/how-it-works-section.tsx
  - apps/web/components/landing/preview-section.tsx
  - apps/web/components/landing/trust-cta-section.tsx
  - apps/web/components/landing/job-preview-card.tsx
autonomous: true

must_haves:
  truths:
    - "랜딩 페이지에서 서비스 가치를 한눈에 파악할 수 있다"
    - "Hero 섹션에서 CTA 버튼(공고 둘러보기, 구인글 올리기)이 클릭 가능하다"
    - "구직자/구인자 각각에게 좋은 점 3가지가 표시된다"
    - "이용 방법 3단계가 안내된다"
    - "실제 공고가 미리보기 카드로 표시되고 국적 필터가 동작한다"
  artifacts:
    - path: "apps/web/app/page.tsx"
      provides: "Landing page with all sections"
      min_lines: 30
    - path: "apps/web/components/landing/hero-section.tsx"
      provides: "Hero with headline, subcopy, CTAs"
      exports: ["HeroSection"]
    - path: "apps/web/components/landing/preview-section.tsx"
      provides: "Job preview cards with nationality filter"
      exports: ["PreviewSection"]
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "@repo/supabase/server"
      via: "createClient for fetching published jobs"
      pattern: "createClient.*from.*job_posts"
    - from: "apps/web/components/landing/preview-section.tsx"
      to: "@repo/lib/constants"
      via: "NATIONALITIES for filter"
      pattern: "NATIONALITIES"
---

<objective>
Create the landing page sections for PotenHire that communicate service value to both job seekers and employers.

Purpose: Convert visitors to users by clearly presenting what PotenHire offers and guiding them to browse jobs or post openings.
Output: Complete landing page at `/` with Hero, Benefits, How It Works, Preview, and Trust sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-landing-page/06-CONTEXT.md
@.planning/phases/06-landing-page/06-RESEARCH.md

# Reference existing patterns
@apps/web/app/(main)/jobs/page.tsx
@apps/web/components/jobs/job-list-filters.tsx
@apps/web/components/jobs/job-row.tsx
@apps/web/components/ui/button.tsx
@apps/web/components/ui/card.tsx
@apps/web/components/ui/badge.tsx
@apps/web/components/ui/select.tsx
@packages/lib/src/constants/nationalities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page section components</name>
  <files>
    apps/web/components/landing/hero-section.tsx
    apps/web/components/landing/why-employers-section.tsx
    apps/web/components/landing/why-talent-section.tsx
    apps/web/components/landing/how-it-works-section.tsx
    apps/web/components/landing/trust-cta-section.tsx
  </files>
  <action>
Create 5 landing section components under `apps/web/components/landing/`:

1. **hero-section.tsx** (Server Component):
   - Service name: "PotenHire" (text logo, no image)
   - Headline tone: "한국인 같은 외국인" concept - emphasize Korean-speaking, culturally-aware foreign talent
   - Subcopy explaining the platform connects employers with Korean-speaking foreign workers
   - Two CTA buttons:
     - Primary: "공고 둘러보기" -> links to `/jobs`
     - Secondary/outline: "구인글 올리기" -> links to `/employer/new-post`
   - Background: CSS gradient (no image), clean and trustworthy feel
   - Use `Button` from `@/components/ui/button` with `asChild` + `Link`

2. **why-employers-section.tsx** (Server Component):
   - Title: "왜 PotenHire인가요?" or similar
   - 3 benefit cards for employers:
     - Benefit 1: Korean-speaking foreign talent pool (한국어 구사 가능한 인재)
     - Benefit 2: Curated/approved posts only (승인형 공고로 신뢰도 확보)
     - Benefit 3: Simple posting process (간편한 공고 등록)
   - Use `Card` components, Lucide icons for visual

3. **why-talent-section.tsx** (Server Component):
   - 3 benefit cards for job seekers:
     - Benefit 1: Jobs matching your nationality (국적에 맞는 공고)
     - Benefit 2: Korean platform, Korean content (한국어로 소통)
     - Benefit 3: Trustworthy, approved listings only (검증된 공고만)
   - Alternate background color (`bg-muted`) to distinguish from employer section

4. **how-it-works-section.tsx** (Server Component):
   - 3-step visual guide, can show for both roles:
     - Seeker steps: 로그인 → 공고 탐색 → 관심 표시/문의
     - Employer steps: 로그인 → 공고 작성 → 승인 후 게시
   - Use numbered steps with icons
   - Clean layout with `flex` or `grid`

5. **trust-cta-section.tsx** (Server Component):
   - Explain approval-based posting ("모든 공고는 관리자 승인 후 게시됩니다")
   - Re-expose CTAs (same as Hero):
     - "공고 둘러보기" -> `/jobs`
     - "구인글 올리기" -> `/employer/new-post`
   - Different visual treatment from Hero (smaller, centered)

All components should:
- Use consistent section padding: `py-16 md:py-24`
- Use container: `container mx-auto px-4` or similar
- Use semantic color classes (`bg-background`, `bg-muted`, `text-foreground`, `text-muted-foreground`)
- Use `cn()` from `@/lib/utils` for conditional classes
  </action>
  <verify>
Files exist and export named components:
```bash
ls apps/web/components/landing/
# Should show: hero-section.tsx, why-employers-section.tsx, why-talent-section.tsx, how-it-works-section.tsx, trust-cta-section.tsx
```
  </verify>
  <done>All 5 section components exist with proper exports and visual structure</done>
</task>

<task type="auto">
  <name>Task 2: Create preview section with job cards and filter</name>
  <files>
    apps/web/components/landing/preview-section.tsx
    apps/web/components/landing/job-preview-card.tsx
  </files>
  <action>
Create the preview section that shows real job posts with nationality filtering:

1. **job-preview-card.tsx** (Server Component):
   - Simplified job card for landing page (different from JobRow which is table-based)
   - Props: `{ job: JobPreview }` where JobPreview has: id, title, company_name, target_nationality, hiring_status, published_at
   - Display: title, company name, nationality badge, hiring status badge
   - Link to `/jobs` (not `/jobs/[id]` to avoid auth issues for non-logged-in users)
   - Use `Card`, `Badge` components
   - Date formatting same as JobRow pattern

2. **preview-section.tsx** (Client Component - needs filter interactivity):
   - Add `'use client'` directive
   - Props: `{ initialJobs: JobPreview[] }` passed from page.tsx
   - Local state for nationality filter (NOT URL-based like JobListFilters)
   - Filter logic:
     - "전체" shows all jobs
     - Specific nationality shows jobs where `target_nationality === code || target_nationality === 'ANY'`
   - Display 6 jobs max in responsive grid (3 cols on lg, 2 on md, 1 on mobile)
   - Use `Select` from shadcn/ui for filter dropdown
   - Import `NATIONALITIES` from `@repo/lib/constants` (filter out 'ANY' from dropdown as in JobListFilters)
   - Section title like "최신 채용 공고" or "공고 미리보기"
   - CTA button at bottom: "전체 공고 보기" -> `/jobs`
  </action>
  <verify>
```bash
grep -l "use client" apps/web/components/landing/preview-section.tsx
# Should find the file (confirms it's a client component)
grep "NATIONALITIES" apps/web/components/landing/preview-section.tsx
# Should find the import
```
  </verify>
  <done>Preview section displays job cards with working nationality filter, JobPreviewCard renders job data</done>
</task>

<task type="auto">
  <name>Task 3: Assemble landing page</name>
  <files>
    apps/web/app/page.tsx
  </files>
  <action>
Replace the existing test page at `apps/web/app/page.tsx` with the full landing page:

1. Make it an async Server Component (already is)
2. Fetch latest 6 published, hiring jobs from Supabase:
   ```typescript
   const { data: previewJobs } = await supabase
     .from('job_posts')
     .select('id, title, company_name, target_nationality, hiring_status, published_at')
     .eq('review_status', 'published')
     .eq('hiring_status', 'hiring')
     .order('published_at', { ascending: false })
     .limit(6)
   ```
3. Import and render all sections in order:
   - HeroSection
   - WhyEmployersSection
   - WhyTalentSection
   - HowItWorksSection
   - PreviewSection (pass `initialJobs={previewJobs || []}`)
   - TrustCtaSection
4. Wrap in `<main>` with no additional layout (root layout is minimal)
5. Update metadata for landing page:
   - title: "PotenHire - 한국어 가능한 외국인 채용 플랫폼"
   - description: Appropriate SEO description
  </action>
  <verify>
```bash
cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm dev --filter=web &
sleep 5
curl -s http://localhost:3000 | grep -o "PotenHire" | head -1
# Should output "PotenHire"
```
Then manually verify: http://localhost:3000 shows landing page with all sections
  </verify>
  <done>Landing page at `/` displays all sections, CTAs link correctly, preview shows real jobs with working filter</done>
</task>

</tasks>

<verification>
1. `pnpm build --filter=web` passes without errors
2. Landing page at `http://localhost:3000` renders all 6 sections
3. Hero CTAs navigate to `/jobs` and `/employer/new-post`
4. Preview section shows job cards from database
5. Nationality filter in preview section filters cards client-side
6. Responsive layout works (mobile, tablet, desktop)
</verification>

<success_criteria>
- LAND-01: Hero section with headline, subcopy, and two CTAs visible
- LAND-02: Why Employers section shows 3 benefits
- LAND-03: Why Talent section shows 3 benefits
- LAND-04: How it Works section shows step-by-step guide
- LAND-05: Preview section shows job cards with nationality filter
- LAND-06: Trust & CTA section shows approval explanation and CTAs
- All sections render without console errors
- Page loads in under 3 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/06-landing-page/06-01-SUMMARY.md`
</output>
