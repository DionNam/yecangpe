---
phase: 08-user-flow-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-user-flow-verification/execution/seeker-journey-results.md
  - .planning/phases/08-user-flow-verification/issues/bug-tracker.md
  - .planning/phases/08-user-flow-verification/issues/prd-mismatches.md
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Job listing page displays correctly without login"
    - "Clicking job item triggers login prompt when not authenticated"
    - "Google OAuth login flow completes successfully"
    - "After OAuth, user completes seeker onboarding and is redirected to job list"
    - "Authenticated user can access job detail page and view full content"
    - "User can toggle heart (like) on job detail page"
    - "Liked jobs appear in user's profile page"
    - "View count increases when job detail is accessed"
    - "User flow behavior matches PRD specifications"
  artifacts:
    - path: ".planning/phases/08-user-flow-verification/execution/seeker-journey-results.md"
      provides: "UAT execution results with pass/fail status for 10 seeker test cases"
      min_lines: 200
    - path: ".planning/phases/08-user-flow-verification/issues/bug-tracker.md"
      provides: "Discovered bugs with severity and reproduction steps"
      min_lines: 10
    - path: ".planning/phases/08-user-flow-verification/issues/prd-mismatches.md"
      provides: "PRD vs implementation gaps discovered during testing"
      min_lines: 10
  key_links:
    - from: ".planning/phases/07-uat-test-case-design/test-cases/01-seeker-journey.md"
      to: ".planning/phases/08-user-flow-verification/execution/seeker-journey-results.md"
      via: "test case definitions drive UAT execution"
      pattern: "UAT-SEEK-\\d{2}"
    - from: ".planning/phases/07-uat-test-case-design/test-data/seed-uat-data.sql"
      to: "Supabase database"
      via: "seed script run before UAT execution"
      pattern: "seeker\\+uat@hire-foreigner.test"
    - from: "Chrome browser with claude-in-chrome extension"
      to: "http://localhost:3000"
      via: "semi-automated browser testing"
      pattern: "natural language navigation commands"
---

<objective>
Execute seeker user journey UAT (10 test cases) using Chrome MCP browser automation to verify core user flow matches PRD specifications.

Purpose: Validate that the implemented seeker experience (authentication, onboarding, job browsing, engagement, profile management) behaves exactly as specified in the PRD, catching any functional bugs or specification mismatches before considering v1.0 production-ready.

Output: Documented test execution results with pass/fail status for all 10 seeker journey test cases, plus bug tracker and PRD mismatch log for any issues discovered.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-user-flow-verification/08-RESEARCH.md
@.planning/phases/07-uat-test-case-design/test-cases/01-seeker-journey.md
@.planning/phases/07-uat-test-case-design/test-data/seed-uat-data.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup UAT Execution Environment</name>
  <files>
    .planning/phases/08-user-flow-verification/execution/seeker-journey-results.md
    .planning/phases/08-user-flow-verification/issues/bug-tracker.md
    .planning/phases/08-user-flow-verification/issues/prd-mismatches.md
  </files>
  <action>
Create directory structure for UAT execution tracking:

1. Create `execution/` directory with seeker-journey-results.md template:
   - Copy test case structure from 01-seeker-journey.md (UAT-SEEK-01 through UAT-SEEK-10)
   - Add execution tracking fields: Status (Pass/Fail), Executed By, Execution Date, Actual Result, Defects Found
   - Include test case metadata: Requirement IDs, Priority, Test Type, Preconditions, Test Steps, Expected Outcome

2. Create `issues/` directory with two tracking files:
   - bug-tracker.md: Table with columns (Bug ID, Test Case, Severity, Description, Reproduction Steps, Status)
   - prd-mismatches.md: Table with columns (Gap ID, PRD Requirement, Actual Behavior, Impact, Remediation)

3. Run seed-uat-data.sql via Supabase SQL Editor:
   - Open https://xztfqnznwcgjjbpyuchf.supabase.co
   - Navigate to SQL Editor
   - Paste contents of .planning/phases/07-uat-test-case-design/test-data/seed-uat-data.sql
   - Execute script (idempotent - safe to run multiple times)
   - Verify success by checking VERIFICATION section output (should show 4 users, 5 jobs, 1 like, 1 config)

4. Start local development servers:
   - `cd /Users/2303-pc02/potenlab/hire_foreigner && pnpm dev`
   - Verify apps/web runs on http://localhost:3000
   - Verify apps/admin runs on http://localhost:3001
   - Check no compilation errors in terminal

IMPORTANT: Do NOT start Chrome browser or run UAT yet. This task only sets up environment. UAT execution happens in checkpoint task.
  </action>
  <verify>
    - `ls .planning/phases/08-user-flow-verification/execution/seeker-journey-results.md` exists
    - `ls .planning/phases/08-user-flow-verification/issues/bug-tracker.md` exists
    - `ls .planning/phases/08-user-flow-verification/issues/prd-mismatches.md` exists
    - Supabase SQL Editor shows success output with correct counts (4 users, 5 jobs, 1 like, 1 config)
    - `curl http://localhost:3000` returns 200 OK
    - `curl http://localhost:3001` returns 200 OK
  </verify>
  <done>
    - UAT execution directory structure created with seeker-journey-results.md template
    - Issue tracking files (bug-tracker.md, prd-mismatches.md) created with table headers
    - seed-uat-data.sql executed successfully in Supabase (test data ready)
    - Development servers running on ports 3000 and 3001 without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Semi-automated UAT execution of 10 seeker journey test cases (UAT-SEEK-01 through UAT-SEEK-10) using Chrome MCP browser automation.

**Execution Pattern:**
This is human-in-the-loop browser automation. You will:
1. Start Claude Code with Chrome integration: `claude --chrome`
2. For each test case, give Claude natural language commands to navigate and verify
3. Manually complete OAuth authentication when prompted (Claude cannot automate Google login)
4. Manually dismiss any JavaScript alert/confirm dialogs (they block Claude's automation)
5. Claude reads DOM, checks console logs, and reports findings
6. You document actual results in seeker-journey-results.md

**Test Cases to Execute:**
- UAT-SEEK-01: Google OAuth login flow (manual auth + automated verification)
- UAT-SEEK-02: Seeker onboarding form completion
- UAT-SEEK-03: Public job list access without login
- UAT-SEEK-04: Nationality filter functionality
- UAT-SEEK-05: Login-required job detail access
- UAT-SEEK-06: View count increase on job detail access
- UAT-SEEK-07: Heart (like) toggle functionality with Optimistic UI
- UAT-SEEK-08: Liked jobs appear in profile page
- UAT-SEEK-09: Profile information editing
- UAT-SEEK-10: Unauthenticated job detail access blocked
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. Ensure seed data loaded: Open Supabase (https://xztfqnznwcgjjbpyuchf.supabase.co) and verify table users has 4 records with emails containing "+uat"
2. Ensure dev servers running: http://localhost:3000 and http://localhost:3001 both respond
3. Have Google account ready for OAuth testing

**Step 1: Start Chrome Automation**
```bash
# In a new terminal window
cd /Users/2303-pc02/potenlab/hire_foreigner
claude --chrome

# In Claude Code, verify Chrome connection:
/chrome
# Should show "Connected" status
```

**Step 2: Execute Test Cases via Natural Language**

For each test case (UAT-SEEK-01 through UAT-SEEK-10):

1. Read test case definition from `.planning/phases/07-uat-test-case-design/test-cases/01-seeker-journey.md`
2. Tell Claude to execute the test using natural language based on test steps
3. Example for UAT-SEEK-01:
   ```
   "Navigate to http://localhost:3000 and verify landing page loads.
   Click the login button and wait for Google OAuth redirect.
   [Then manually complete Google authentication]
   After I complete OAuth, verify redirect to /onboarding/role-selection page."
   ```
4. Claude navigates, clicks, reads DOM, reports findings
5. When Claude detects OAuth or alert dialog, manually complete the action
6. Claude continues verification after manual step
7. Document actual result in seeker-journey-results.md:
   - Mark status: Pass or Fail
   - Record execution date and actual behavior
   - If failed, create bug tracker entry with severity and reproduction steps
   - If PRD mismatch found, create entry in prd-mismatches.md

**Step 3: Example Natural Language Commands**

UAT-SEEK-03 (Public job list):
```
"Navigate to http://localhost:3000 and click the '공고 둘러보기' CTA button.
Verify the job list page loads and displays at least 5 published job posts in a table.
Check that columns include: Status badge, Job title, Published date, View count.
Then click on the first job title and verify a login modal appears."
```

UAT-SEEK-07 (Heart toggle):
```
"Navigate to http://localhost:3000, login as seeker (I'll complete OAuth),
complete onboarding if prompted, navigate to job list, click first job title.
On job detail page, click the heart icon and verify optimistic UI update
(heart fills immediately). Check console for no errors."
```

**Step 4: Document Results**

For each test case in seeker-journey-results.md:
- **Status:** [x] Pass or [ ] Fail
- **Executed By:** Your name
- **Execution Date:** 2026-01-21 HH:MM KST
- **Actual Result:** 2-3 sentences describing what happened
- **Defects Found:** Link to bug tracker entry if test failed or unexpected behavior

If bugs found:
- Add to bug-tracker.md: BUG-SEEK-XXX with severity (Critical/High/Medium/Low), description, reproduction steps

If PRD mismatch found:
- Add to prd-mismatches.md: GAP-SEEK-XXX with PRD requirement ID, actual behavior, impact assessment

**Expected Outcome:**
- All 10 test cases executed and documented
- Pass/Fail status for each test case
- Any discovered bugs logged in bug-tracker.md
- Any PRD mismatches logged in prd-mismatches.md
- Summary of test results (e.g., "8/10 passed, 2 bugs found")

**Tips:**
- If Claude stops responding, check for modal dialogs (alert/confirm) and dismiss them
- OAuth cannot be automated - you must manually log in via Google each time
- Use "wait for element to be visible" instead of "wait 5 seconds" for dynamic content
- Ask Claude to "check console for errors" rather than "show all console logs" to reduce noise
- If a test case is complex, break it into smaller natural language commands
- Take breaks between test cases - Chrome automation can be intensive
  </how-to-verify>
  <resume-signal>
Type one of the following:

**If UAT execution complete:**
"uat-complete" followed by summary of results (e.g., "8/10 passed, 2 bugs found in bug-tracker.md")

**If blocking issues found:**
"blocker" followed by description of critical issue that prevents continuing

**If need to defer execution:**
"defer" followed by reason (e.g., "Chrome MCP not set up", "Need production database access")
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Analyze and Summarize Test Results</name>
  <files>
    .planning/phases/08-user-flow-verification/execution/seeker-journey-results.md
    .planning/phases/08-user-flow-verification/issues/bug-tracker.md
    .planning/phases/08-user-flow-verification/issues/prd-mismatches.md
  </files>
  <action>
Read execution results and create analysis summary:

1. Count pass/fail for seeker journey test cases:
   - Total test cases: 10
   - Passed: N (UAT-SEEK-XX entries marked Pass)
   - Failed: M (UAT-SEEK-XX entries marked Fail)
   - Pass rate: N/10 * 100%

2. Analyze bugs by severity:
   - Count Critical bugs (blocks core functionality)
   - Count High bugs (impacts user experience significantly)
   - Count Medium bugs (minor UX issues or edge cases)
   - Count Low bugs (cosmetic or documentation issues)

3. Analyze PRD mismatches by impact:
   - Count mismatches that require code changes
   - Count mismatches that require PRD clarification
   - Count mismatches that are acceptable deviations

4. Generate recommendations:
   - If pass rate < 80%: Recommend Phase 9 (Bug Fixes) before production
   - If Critical/High bugs > 0: List must-fix issues before production
   - If PRD mismatches found: Recommend stakeholder review of gaps

5. Add summary section to top of seeker-journey-results.md:
   ```markdown
   ## Test Execution Summary

   **Executed:** 2026-01-21
   **Total Test Cases:** 10
   **Passed:** N
   **Failed:** M
   **Pass Rate:** N%

   **Bugs Found:** X (Y critical, Z high)
   **PRD Mismatches:** W

   **Recommendation:** [Pass for production / Requires bug fixes / Requires PRD review]
   ```

IMPORTANT: This task analyzes results. Do NOT re-run tests or modify test data.
  </action>
  <verify>
    - `grep "## Test Execution Summary" .planning/phases/08-user-flow-verification/execution/seeker-journey-results.md` returns match
    - Summary shows correct pass/fail counts
    - bug-tracker.md has severity assigned to each bug
    - prd-mismatches.md has impact assessment for each gap
  </verify>
  <done>
    - Test execution summary added to seeker-journey-results.md with pass/fail stats
    - Bugs categorized by severity in bug-tracker.md
    - PRD mismatches categorized by impact in prd-mismatches.md
    - Clear recommendation provided (production-ready vs requires fixes)
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Execution tracking exists:**
   - seeker-journey-results.md contains all 10 test cases with pass/fail status
   - Each test case has: Executed By, Execution Date, Actual Result fields filled
   - Test execution summary shows pass/fail counts and pass rate

2. **Issues documented:**
   - bug-tracker.md lists any bugs discovered during testing
   - prd-mismatches.md lists any gaps between PRD and implementation
   - Both files have severity/impact assessment

3. **Recommendation clear:**
   - Summary states whether seeker flow is production-ready
   - Critical/High bugs are clearly identified if present
   - Next steps are clear (ship vs fix bugs vs review PRD gaps)

4. **Traceability maintained:**
   - Each test result references original test case (UAT-SEEK-XX)
   - Bug tracker entries reference failing test cases
   - PRD mismatches reference specific PRD requirements
</verification>

<success_criteria>
**Seeker user journey verified when:**
- All 10 seeker test cases (UAT-SEEK-01 through UAT-SEEK-10) executed and documented
- Pass/fail status recorded for each test case with actual results
- Any bugs or PRD mismatches logged in tracking files with severity/impact
- Test execution summary provides clear recommendation (production-ready or requires fixes)
- Team has evidence-based decision point: ship seeker flow or fix issues first

**Measurable outcomes:**
- 10/10 test cases have execution status (Pass or Fail)
- Pass rate calculated (target: 80%+ for production-ready)
- Bug count by severity (Critical, High, Medium, Low)
- PRD mismatch count with impact assessment
- Recommendation stated: "Production-ready" or "Requires [N] fixes before production"
</success_criteria>

<output>
After completion, create `.planning/phases/08-user-flow-verification/08-01-SUMMARY.md`
</output>
