---
phase: 02-authentication
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/web/app/(onboarding)/onboarding/page.tsx
  - apps/web/app/(onboarding)/layout.tsx
  - apps/web/components/auth/logout-button.tsx
autonomous: true

must_haves:
  truths:
    - "First-time user sees role selection page at /onboarding"
    - "User can choose between seeker and employer roles"
    - "Selected role navigates to corresponding profile form"
    - "User can log out and return to login page"
  artifacts:
    - path: "apps/web/app/(onboarding)/onboarding/page.tsx"
      provides: "Role selection UI with seeker/employer options"
      min_lines: 30
    - path: "apps/web/app/(onboarding)/layout.tsx"
      provides: "Onboarding layout wrapper"
      min_lines: 5
    - path: "apps/web/components/auth/logout-button.tsx"
      provides: "Sign out button component"
      min_lines: 15
  key_links:
    - from: "apps/web/app/(onboarding)/onboarding/page.tsx"
      to: "/onboarding/seeker"
      via: "Link navigation"
      pattern: "href=.*onboarding/seeker"
    - from: "apps/web/app/(onboarding)/onboarding/page.tsx"
      to: "/onboarding/employer"
      via: "Link navigation"
      pattern: "href=.*onboarding/employer"
    - from: "apps/web/components/auth/logout-button.tsx"
      to: "supabase.auth.signOut"
      via: "client-side sign out"
      pattern: "signOut"
---

<objective>
Create role selection page and logout button for the onboarding flow.

Purpose: Allow first-time users to choose their role (seeker or employer) before completing profile-specific onboarding.

Output: Role selection page at /onboarding with clear seeker/employer options and logout capability.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md

# UI components from 02-01
@apps/web/components/ui/button.tsx
@apps/web/components/ui/card.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create role selection page</name>
  <files>
    apps/web/app/(onboarding)/onboarding/page.tsx
    apps/web/app/(onboarding)/layout.tsx
  </files>
  <action>
    Create directory structure:
    ```bash
    mkdir -p apps/web/app/\(onboarding\)/onboarding
    ```

    Create `apps/web/app/(onboarding)/layout.tsx`:
    - Simple layout that wraps children
    - Can add centered container styling if desired
    - Export as default function OnboardingLayout

    Create `apps/web/app/(onboarding)/onboarding/page.tsx`:
    - Import Link from next/link
    - Import Card, CardContent, CardDescription, CardHeader, CardTitle from @/components/ui/card
    - Import Button from @/components/ui/button

    Layout:
    - min-h-screen flex items-center justify-center p-4
    - Card with max-w-md width

    CardHeader:
    - Title: "어떤 목적으로 사용하시나요?"
    - Description: "가입 유형을 선택해주세요"

    CardContent with space-y-4:
    - Two Button elements with asChild variant="outline"
    - Each wraps a Link:

    Seeker option:
    - href: "/onboarding/seeker"
    - Button classes: w-full h-auto py-6
    - Inner div with text-left:
      - Font-semibold: "일자리를 찾고 있어요"
      - text-sm text-muted-foreground: "구직자로 가입합니다"

    Employer option:
    - href: "/onboarding/employer"
    - Same button structure
      - Font-semibold: "인재를 찾고 있어요"
      - text-sm text-muted-foreground: "구인자로 가입합니다"
  </action>
  <verify>
    - `pnpm build` succeeds in apps/web
    - Visit http://localhost:3000/onboarding (after auth) shows role selection
    - Both buttons link to correct paths
    - Layout is centered and visually correct
  </verify>
  <done>
    Role selection page displays two clear options for seeker and employer roles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create logout button component</name>
  <files>
    apps/web/components/auth/logout-button.tsx
  </files>
  <action>
    Create `apps/web/components/auth/logout-button.tsx`:
    - 'use client' directive
    - Import createClient from @repo/supabase/client
    - Import useRouter from next/navigation
    - Import Button from @/components/ui/button

    Define LogoutButton component:
    - const router = useRouter()

    handleLogout async function:
    - const supabase = createClient()
    - await supabase.auth.signOut()
    - router.push('/login')
    - router.refresh() // Clear server cache

    Return:
    - Button with variant="ghost" and onClick={handleLogout}
    - Text: "로그아웃"

    Export LogoutButton as named export.
  </action>
  <verify>
    - File exists at apps/web/components/auth/logout-button.tsx
    - TypeScript compilation succeeds
    - Component can be imported and rendered
  </verify>
  <done>
    Logout button component ready for use in navigation or profile pages.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm build` succeeds at root level
2. `pnpm dev` starts successfully
3. Manually test (requires completing auth flow):
   - After OAuth, new user should see /onboarding
   - Role selection shows seeker and employer options
   - Clicking seeker navigates to /onboarding/seeker (will 404 until 02-03)
   - Clicking employer navigates to /onboarding/employer (will 404 until 02-03)
4. Logout button can be imported and renders correctly
</verification>

<success_criteria>
- AUTH-02 partially complete: Role selection UI built (forms in 02-03)
- Role selection page renders with two clear options
- Navigation links work to seeker/employer paths
- Logout button signs user out and redirects to login
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-02-SUMMARY.md`
</output>
