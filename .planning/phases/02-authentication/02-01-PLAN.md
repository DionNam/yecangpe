---
phase: 02-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/components.json
  - apps/web/app/(auth)/login/page.tsx
  - apps/web/app/(auth)/auth/callback/route.ts
  - apps/web/components/auth/login-button.tsx
  - apps/web/components/ui/button.tsx
  - apps/web/components/ui/card.tsx
  - apps/web/middleware.ts
  - packages/supabase/src/middleware.ts
autonomous: true
user_setup:
  - service: supabase
    why: "Google OAuth provider configuration"
    dashboard_config:
      - task: "Enable Google OAuth provider"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
        details: "Enter Google Client ID and Secret from Google Cloud Console"
      - task: "Add redirect URLs"
        location: "Supabase Dashboard -> Authentication -> URL Configuration"
        details: "Add http://localhost:3000/auth/callback and production URL"
  - service: google-cloud
    why: "OAuth 2.0 credentials"
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID"
        location: "Google Cloud Console -> APIs & Services -> Credentials"
        details: "Web application type. Redirect URI: https://[your-project].supabase.co/auth/v1/callback"

must_haves:
  truths:
    - "User can click Google login button and be redirected to Google"
    - "After Google auth, user is redirected back to the app with session"
    - "Users without profile are redirected to /onboarding"
    - "Session persists across browser refresh"
  artifacts:
    - path: "apps/web/app/(auth)/login/page.tsx"
      provides: "Login page with Google button"
      min_lines: 15
    - path: "apps/web/app/(auth)/auth/callback/route.ts"
      provides: "OAuth callback handler with PKCE exchange"
      exports: ["GET"]
    - path: "apps/web/components/auth/login-button.tsx"
      provides: "Google OAuth trigger component"
      min_lines: 20
    - path: "apps/web/middleware.ts"
      provides: "Enhanced middleware with onboarding redirect"
      min_lines: 30
  key_links:
    - from: "apps/web/components/auth/login-button.tsx"
      to: "supabase.auth.signInWithOAuth"
      via: "client-side OAuth trigger"
      pattern: "signInWithOAuth.*google"
    - from: "apps/web/app/(auth)/auth/callback/route.ts"
      to: "supabase.auth.exchangeCodeForSession"
      via: "PKCE code exchange"
      pattern: "exchangeCodeForSession"
    - from: "apps/web/middleware.ts"
      to: "users table"
      via: "role check query"
      pattern: "from\\('users'\\)"
---

<objective>
Set up Google OAuth authentication with PKCE flow and middleware-based onboarding enforcement.

Purpose: Enable users to sign in with Google and ensure incomplete users are redirected to onboarding before accessing protected routes.

Output: Working OAuth flow from login button through callback to session, with middleware that redirects users without profiles to /onboarding.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md

# Existing code to reference
@packages/supabase/src/client.ts
@packages/supabase/src/server.ts
@packages/supabase/src/middleware.ts
@apps/web/middleware.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and add shadcn/ui components</name>
  <files>
    apps/web/package.json
    apps/web/components.json
    apps/web/lib/utils.ts
    apps/web/components/ui/button.tsx
    apps/web/components/ui/card.tsx
  </files>
  <action>
    Install form dependencies in apps/web:
    ```bash
    cd apps/web && pnpm add react-hook-form @hookform/resolvers zod
    ```

    Initialize shadcn/ui (if not already done):
    ```bash
    cd apps/web && pnpm dlx shadcn@latest init
    ```
    When prompted:
    - Style: Default
    - Base color: Slate
    - CSS variables: Yes
    - Tailwind config: tailwind.config.ts (or let it create one)
    - Components: @/components
    - Utils: @/lib/utils

    Add required shadcn components:
    ```bash
    cd apps/web && pnpm dlx shadcn@latest add button card
    ```

    These provide the UI primitives for login page and onboarding forms.
  </action>
  <verify>
    - `pnpm ls react-hook-form @hookform/resolvers zod` shows versions installed
    - `apps/web/components/ui/button.tsx` exists
    - `apps/web/components/ui/card.tsx` exists
    - `apps/web/lib/utils.ts` exists with cn() function
    - `pnpm build` succeeds in apps/web
  </verify>
  <done>
    Form libraries installed, shadcn/ui initialized, button and card components available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OAuth callback route and login page</name>
  <files>
    apps/web/app/(auth)/auth/callback/route.ts
    apps/web/app/(auth)/login/page.tsx
    apps/web/components/auth/login-button.tsx
  </files>
  <action>
    Create directory structure:
    ```bash
    mkdir -p apps/web/app/\(auth\)/auth/callback
    mkdir -p apps/web/app/\(auth\)/login
    mkdir -p apps/web/components/auth
    ```

    Create `apps/web/app/(auth)/auth/callback/route.ts`:
    - Import createClient from @repo/supabase/server
    - GET handler receives auth code from URL params
    - Call `supabase.auth.exchangeCodeForSession(code)` for PKCE exchange
    - On success, check if user has profile:
      - Query users table for role
      - If no role: redirect to /onboarding
      - If has role: redirect to /
    - On error: redirect to /login?error=auth

    Create `apps/web/components/auth/login-button.tsx`:
    - 'use client' component
    - Import createClient from @repo/supabase/client
    - Import Button from @/components/ui/button
    - handleLogin calls signInWithOAuth with:
      - provider: 'google'
      - options.redirectTo: `${window.location.origin}/auth/callback`
      - options.queryParams: { access_type: 'offline', prompt: 'consent' }
    - Render Button with Google icon and text "Google로 계속하기"
    - Use inline SVG for Google icon (no external dependencies)

    Create `apps/web/app/(auth)/login/page.tsx`:
    - Import Card components from @/components/ui/card
    - Import LoginButton from @/components/auth/login-button
    - Center layout with min-h-screen flex items-center justify-center
    - Card with title "외국인 구인구직" and description
    - LoginButton inside CardContent
  </action>
  <verify>
    - `curl http://localhost:3000/login` returns 200 (after dev server)
    - `apps/web/app/(auth)/auth/callback/route.ts` exports GET function
    - `apps/web/components/auth/login-button.tsx` contains signInWithOAuth
    - TypeScript compilation succeeds: `pnpm build` in apps/web
  </verify>
  <done>
    OAuth callback handler exchanges code for session and redirects appropriately. Login page displays with Google login button.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enhance middleware with onboarding enforcement</name>
  <files>
    packages/supabase/src/middleware.ts
    apps/web/middleware.ts
  </files>
  <action>
    Update `packages/supabase/src/middleware.ts`:
    - Keep existing updateSession function
    - Ensure it returns { supabaseResponse, user, supabase } (add supabase client to return)
    - This allows consuming middleware to make additional queries

    Update `apps/web/middleware.ts`:
    - Define route arrays:
      - publicRoutes: ['/', '/login', '/auth/callback', '/jobs'] (pages accessible without auth)
      - onboardingRoutes: ['/onboarding'] (routes that don't require profile completion)
    - Get { supabaseResponse, user } from updateSession(request)
    - If pathname matches publicRoutes or starts with /auth/: return supabaseResponse
    - If no user and not public route: redirect to /login
    - If user exists and not onboarding route:
      - Create server client using createServerClient from @supabase/ssr
      - Query users table: `select('role').eq('id', user.id).single()`
      - If no role (null): redirect to /onboarding
    - Return supabaseResponse

    Handle edge case: /onboarding/* routes should allow authenticated users without profile.

    Avoid infinite redirect loops:
    - Don't redirect /login to /login
    - Don't redirect /onboarding to /onboarding
    - Don't redirect /auth/callback to /login
  </action>
  <verify>
    - `pnpm build` succeeds in root (builds all packages)
    - Start dev server: `pnpm dev`
    - Visit http://localhost:3000/login - should show login page (public)
    - Visit http://localhost:3000/some-protected-page without auth - should redirect to /login
    - TypeScript compilation passes with no errors
  </verify>
  <done>
    Middleware enforces authentication for protected routes and redirects incomplete users to onboarding.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm build` succeeds at root level
2. `pnpm dev` starts both apps
3. Visit http://localhost:3000/login - see login page with Google button
4. Click Google button - redirects to Google OAuth (may fail without Supabase config)
5. Visit protected route without auth - redirects to /login
6. Session cookies are set after OAuth flow (requires Supabase Google OAuth configured)
</verification>

<success_criteria>
- AUTH-01 partially complete: OAuth flow implemented (needs dashboard config for full test)
- AUTH-05 complete: Session persistence via @supabase/ssr middleware
- Login page renders with Google button
- Callback route exchanges code for session
- Middleware redirects unauthenticated users to /login
- Middleware redirects users without profile to /onboarding
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-01-SUMMARY.md`
</output>
