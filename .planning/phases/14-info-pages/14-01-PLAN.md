---
phase: 14-info-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/metadata.ts
  - apps/web/components/info-pages/pain-point-section.tsx
  - apps/web/components/info-pages/value-proposition-section.tsx
  - apps/web/components/info-pages/step-guide-section.tsx
  - apps/web/components/info-pages/benefits-card-grid.tsx
  - apps/web/components/info-pages/final-cta-section.tsx
autonomous: true

must_haves:
  truths:
    - "Shared info-page components are importable and render without errors"
    - "Metadata helper generates consistent page-specific SEO metadata"
    - "Components accept variant props to serve both job-seeker and employer contexts"
  artifacts:
    - path: "apps/web/lib/metadata.ts"
      provides: "generateInfoPageMetadata helper function"
      exports: ["generateInfoPageMetadata"]
    - path: "apps/web/components/info-pages/pain-point-section.tsx"
      provides: "Pain point display with red X icons"
      exports: ["PainPointSection"]
    - path: "apps/web/components/info-pages/value-proposition-section.tsx"
      provides: "4-card benefit grid with icons"
      exports: ["ValuePropositionSection"]
    - path: "apps/web/components/info-pages/step-guide-section.tsx"
      provides: "3-step guide with seeker/employer variants"
      exports: ["StepGuideSection"]
    - path: "apps/web/components/info-pages/benefits-card-grid.tsx"
      provides: "4-card benefits grid for employer page"
      exports: ["BenefitsCardGrid"]
    - path: "apps/web/components/info-pages/final-cta-section.tsx"
      provides: "Conversion CTA section with seeker/employer variants"
      exports: ["FinalCTASection"]
  key_links:
    - from: "apps/web/components/info-pages/step-guide-section.tsx"
      to: "variant prop"
      via: "TypeScript union type"
      pattern: "variant.*seeker.*employer"
    - from: "apps/web/lib/metadata.ts"
      to: "Next.js Metadata type"
      via: "import from next"
      pattern: "import.*Metadata.*from.*next"
---

<objective>
Create shared section components for info pages and a metadata helper utility.

Purpose: Foundation components needed by /job-seekers, /employers, /about, and /faq pages. Building these first avoids duplication and ensures consistency across all info pages.

Output: 5 new React components in `components/info-pages/` directory + 1 metadata utility in `lib/metadata.ts`.
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-info-pages/14-RESEARCH.md

Key references from existing codebase:
@apps/web/components/landing/hero-section.tsx (pattern for 'use client' + motion animations)
@apps/web/components/landing/faq-section.tsx (pattern for Radix UI Accordion)
@apps/web/components/landing/social-proof-section.tsx (pattern for animated counter reuse)
@apps/web/app/page.tsx (pattern for metadata export and ISR)
@apps/web/app/layout.tsx (root layout with metadataBase)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create metadata helper utility</name>
  <files>apps/web/lib/metadata.ts</files>
  <action>
Create `apps/web/lib/metadata.ts` with a `generateInfoPageMetadata` helper function.

The function should accept `{ title: string, description: string, path: string }` and return a Next.js `Metadata` object with:
- `title`: Uses template format from root layout (just set `title` field, root layout has `template: '%s | HangulJobs'`)
- `description`: Passed through
- `alternates.canonical`: The path
- `openGraph`: title, description, url (baseUrl + path), type 'website', locale 'ko_KR'
- `twitter`: card 'summary_large_image', title, description

Use `process.env.NEXT_PUBLIC_SITE_URL || 'https://hanguljobs.com'` for baseUrl, matching existing pattern in `apps/web/app/page.tsx`.

Export the function as named export.
  </action>
  <verify>Run `npx tsc --noEmit -p apps/web/tsconfig.json` - no errors related to metadata.ts</verify>
  <done>generateInfoPageMetadata function exists, is type-safe, and generates Metadata objects with title, description, OG, and Twitter fields</done>
</task>

<task type="auto">
  <name>Task 2: Create info-page section components</name>
  <files>
    apps/web/components/info-pages/pain-point-section.tsx
    apps/web/components/info-pages/value-proposition-section.tsx
    apps/web/components/info-pages/step-guide-section.tsx
    apps/web/components/info-pages/benefits-card-grid.tsx
    apps/web/components/info-pages/final-cta-section.tsx
  </files>
  <action>
Create 5 client components in `apps/web/components/info-pages/`. All use `'use client'` directive and motion animations from `motion/react` (NOT framer-motion - the project uses the `motion` package). All use Lucide React icons.

**1. pain-point-section.tsx** - `PainPointSection` component:
- Heading: "해외에서 한국어 관련 일자리 찾기, 이런 어려움 겪으셨나요?"
- Display 4 pain points in a 2-column grid (md:grid-cols-2):
  1. "해외에서 한국어 관련 일자리 정보를 찾기 어렵다"
  2. "리크루터 수수료가 부담스럽다"
  3. "언어 요구 수준이 명확하지 않다"
  4. "원격 근무 옵션을 찾기 힘들다"
- Each item: white card with red-200 border, X icon (Lucide) in red-500, text in gray-700
- Background: bg-slate-50, section padding py-20
- Motion: whileInView fade-in for heading and staggered items (delay index * 0.1)

**2. value-proposition-section.tsx** - `ValuePropositionSection` component:
- Heading: "HangulJobs가 도와드립니다"
- 4-card grid (md:grid-cols-2 lg:grid-cols-4) with:
  1. Target icon - "타겟 인력풀" / "한국어 구사자만을 위한 전문 잡보드" (blue)
  2. Briefcase icon - "다양한 고용 형태" / "정규직, 파트타임, 프리랜서, 인턴십 등" (emerald)
  3. Globe icon - "글로벌 커버리지" / "전 세계 어디서든 일자리 검색 가능" (amber)
  4. CheckCircle icon - "검증된 공고" / "모든 공고는 관리자 승인 후 게시" (purple)
- Each card: bg-slate-50 rounded-xl p-6 with hover:shadow-md transition
- Icon container: w-12 h-12 rounded-lg with colored bg + matching text color
- Background: bg-white, section padding py-20

**3. step-guide-section.tsx** - `StepGuideSection` component with `variant: 'seeker' | 'employer'` prop:
- Heading: variant === 'seeker' ? '이용 방법' : '공고 게시 절차'
- 3-step horizontal layout (md:grid-cols-3):
  - Seeker steps:
    1. Search icon, "잡 검색", "국가, 고용형태, 카테고리 등 필터로 맞춤 검색", blue
    2. Heart icon, "관심 표시", "마음에 드는 공고에 하트를 눌러 저장", emerald
    3. Send icon, "직접 지원", "공고 상세에서 바로 지원하세요", amber
  - Employer steps:
    1. LogIn icon, "무료 가입", "Google 계정으로 간편하게 시작", blue
    2. FileText icon, "공고 작성", "5분이면 완료되는 간단한 폼", emerald
    3. Users icon, "인재 매칭", "적합한 인재가 직접 지원합니다", amber
- Each step: centered icon circle (w-16 h-16 rounded-full), step number badge (absolute positioned, bg-slate-900 text-white w-10 h-10 rounded-full), title, description
- Connector line between steps on desktop (optional: use a dashed border-top on the grid container)
- Motion: staggered whileInView fade-in

**4. benefits-card-grid.tsx** - `BenefitsCardGrid` component (for employer page):
- Heading: "HangulJobs를 선택해야 하는 이유"
- 4-card grid (md:grid-cols-2):
  1. DollarSign icon - "완전 무료" / "공고 게시, 지원자 조회, 통계 확인까지 모든 기능 무료" (blue)
  2. Target icon - "타겟 인재풀" / "한국어를 구사하는 글로벌 인재에게 직접 도달" (emerald)
  3. Shield icon - "검증된 플랫폼" / "관리자 승인 프로세스로 공고 품질 보장" (amber)
  4. BarChart icon - "실시간 통계" / "공고별 조회수, 관심수를 실시간으로 확인" (purple)
- Cards: bg-white rounded-xl p-8 border border-slate-200 hover:shadow-md transition
- Large icon circle (w-14 h-14), bold title, description text
- Background: bg-slate-50, section padding py-20

**5. final-cta-section.tsx** - `FinalCTASection` component with `variant: 'seeker' | 'employer'` prop:
- Seeker variant:
  - Heading: "지금 바로 시작하세요"
  - Subtitle: "전 세계 한국어 일자리를 무료로 검색하세요"
  - Primary button: "잡 검색하기" -> /jobs (bg-blue-600)
  - Secondary button: "뉴스레터 구독" -> #newsletter (bg-white border)
- Employer variant:
  - Heading: "무료로 공고를 올려보세요"
  - Subtitle: "한국어 가능 인재를 찾는 가장 빠른 방법"
  - Primary button: "무료로 공고 올리기" -> /employer/new-post (bg-amber-600)
  - Secondary button: "잡보드 둘러보기" -> /jobs (bg-white border)
- Background: gradient from-slate-900 to-slate-800 with white text
- Dual CTA buttons centered, flex-col sm:flex-row layout
- Motion: fade-in on viewport entry
  </action>
  <verify>Run `npx tsc --noEmit -p apps/web/tsconfig.json` - no TypeScript errors. All 5 component files exist and export named components.</verify>
  <done>5 new section components exist in info-pages/ directory, all use 'use client', motion/react animations, Lucide icons, and Tailwind styling consistent with Phase 13 landing components. StepGuideSection and FinalCTASection accept variant props.</done>
</task>

</tasks>

<verification>
1. `ls apps/web/components/info-pages/` shows 5 .tsx files
2. `ls apps/web/lib/metadata.ts` exists
3. `npx tsc --noEmit -p apps/web/tsconfig.json` passes without errors
4. Each component exports a named function component
5. No duplicate code with existing landing/ components
</verification>

<success_criteria>
- 5 new section components created and type-checking passes
- Metadata helper generates proper Next.js Metadata objects
- Components use established patterns (motion/react, Lucide, Tailwind, 'use client')
- StepGuideSection and FinalCTASection accept variant prop for seeker/employer differentiation
</success_criteria>

<output>
After completion, create `.planning/phases/14-info-pages/14-01-SUMMARY.md`
</output>
