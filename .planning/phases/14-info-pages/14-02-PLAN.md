---
phase: 14-info-pages
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/web/app/(marketing)/job-seekers/page.tsx
autonomous: true

must_haves:
  truths:
    - "/job-seekers page renders with Hero, Pain Point, Value Proposition, Step Guide, Latest Jobs, Filter Categories, FAQ, and Final CTA sections"
    - "Page fetches latest 8 published jobs server-side with ISR revalidation"
    - "Page has unique SEO metadata (title, description, OpenGraph)"
    - "Page is responsive on mobile (single column layout)"
  artifacts:
    - path: "apps/web/app/(marketing)/job-seekers/page.tsx"
      provides: "Job seekers info page with 8 sections"
      exports: ["default", "metadata", "revalidate"]
  key_links:
    - from: "apps/web/app/(marketing)/job-seekers/page.tsx"
      to: "apps/web/components/info-pages/pain-point-section.tsx"
      via: "import"
      pattern: "import.*PainPointSection"
    - from: "apps/web/app/(marketing)/job-seekers/page.tsx"
      to: "apps/web/components/landing/preview-section.tsx"
      via: "import"
      pattern: "import.*PreviewSection"
    - from: "apps/web/app/(marketing)/job-seekers/page.tsx"
      to: "@repo/supabase/server"
      via: "server-side data fetching"
      pattern: "createClient.*from.*supabase/server"
---

<objective>
Create the /job-seekers info page with all 8 required sections.

Purpose: Primary conversion page for job seekers arriving from the landing page hero CTA ("I'm a Job Seeker"). Must demonstrate value, show process, display latest jobs, and drive users to the job board.

Output: Server component page at `/job-seekers` with Hero-style header, Pain Points, Value Propositions, Step Guide, Latest Jobs preview, Filter Categories, FAQ, and Final CTA.
</objective>

<execution_context>
@/Users/2303-pc02/.claude/get-shit-done/workflows/execute-plan.md
@/Users/2303-pc02/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-info-pages/14-RESEARCH.md
@.planning/phases/14-info-pages/14-01-SUMMARY.md

Key file references:
@apps/web/app/page.tsx (pattern for server-side Supabase fetching, ISR, metadata)
@apps/web/app/layout.tsx (root layout - provides SiteHeaderWrapper, no need for separate layout)
@apps/web/components/landing/preview-section.tsx (reuse for latest jobs)
@apps/web/components/landing/filter-category-cards.tsx (reuse for browse categories)
@apps/web/components/landing/faq-section.tsx (reuse pattern for seeker FAQ)
@apps/web/components/landing/footer.tsx (reuse for page footer)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create (marketing) route group and job-seekers page</name>
  <files>apps/web/app/(marketing)/job-seekers/page.tsx</files>
  <action>
Create `apps/web/app/(marketing)/job-seekers/page.tsx` as a **server component** (no 'use client').

The (marketing) route group is a Next.js organizational pattern that does NOT affect URLs. The page will be accessible at `/job-seekers`.

**Metadata** (static export):
```typescript
export const metadata: Metadata = {
  title: 'For Job Seekers - Find Korean-Speaking Jobs Worldwide',
  description: 'Discover global opportunities that value your Korean language skills. Browse verified job postings, filter by location and type, and connect with top employers.',
  keywords: ['Korean speaking jobs', 'job seeker', 'Korean language jobs', 'global opportunities', 'bilingual jobs', '한국어 채용', '해외 취업'],
  openGraph: {
    title: 'For Job Seekers | HangulJobs',
    description: 'Find Korean-speaking jobs worldwide. Browse, filter, and apply to verified opportunities.',
    type: 'website',
    locale: 'ko_KR',
  },
  twitter: {
    card: 'summary_large_image',
    title: 'For Job Seekers | HangulJobs',
    description: 'Discover global opportunities that value your Korean language skills',
  },
  alternates: {
    canonical: '/job-seekers',
  },
}
```

**ISR**: `export const revalidate = 7200` (2 hours - this page fetches job data)

**Server-side data fetching** (same pattern as landing page):
- Fetch latest 8 published, hiring jobs: `supabase.from('job_posts').select('id, title, company_name, job_type, work_location_type, work_location_country, published_at').eq('review_status', 'published').eq('hiring_status', 'hiring').order('published_at', { ascending: false }).limit(8)`
- Use `createClient` from `@repo/supabase/server`

**Page Hero** (NOT reusing HeroSection component - create inline since it's different content):
Build a hero section directly in the page (or as a simple div structure) with:
- Badge: "For Job Seekers" in blue-100/blue-700 pill
- Heading: "전 세계에서 한국어 스킬을 활용한 기회를 찾으세요"
- Subtitle in English: "Find opportunities worldwide that value your Korean skills"
- Dual CTA: "잡 검색하기" -> /jobs (blue-600 primary), "회원가입" -> /login (white outline secondary)
- Trust indicators: "Verified Job Posts" (CheckCircle emerald), "Global Opportunities" (Globe blue), "100% Free" (Sparkles amber)
- Background: gradient from-slate-50 via-white to-blue-50, py-20
- Build as static HTML without motion animations (server component, SEO-friendly)

**Section order in the return JSX:**
1. Hero (inline - styled div with heading, subtitle, CTAs, trust indicators)
2. `<PainPointSection />` from info-pages
3. `<ValuePropositionSection />` from info-pages
4. `<StepGuideSection variant="seeker" />` from info-pages
5. `<PreviewSection initialJobs={previewJobs || []} />` from landing (reuse directly)
6. `<FilterCategoryCards />` from landing (reuse directly)
7. `<SeekerFAQSection />` from info-pages (created in Task 2 below) - import and render directly. This is a client component so it works inside the server component page.

8. `<FinalCTASection variant="seeker" />` from info-pages
9. `<Footer />` from landing

**Important patterns:**
- Import `createClient` from `@repo/supabase/server` (NOT `@supabase/ssr`)
- Import `Metadata` type from `next`
- The root layout already provides SiteHeaderWrapper - do NOT add navigation
- Do NOT create a layout.tsx in the (marketing) directory - root layout handles everything
  </action>
  <verify>
1. `npx tsc --noEmit -p apps/web/tsconfig.json` passes
2. File exists at `apps/web/app/(marketing)/job-seekers/page.tsx`
3. File exports metadata, revalidate, and default function
4. Build and verify SSR: Run `cd apps/web && npx next build` (or dev server), then `curl -s http://localhost:3000/job-seekers | grep -c 'job_type\|company_name\|title'` confirms job data is present in the HTML source before hydration. Alternatively, in dev mode, view page source and verify job titles appear in the raw HTML.
  </verify>
  <done>/job-seekers page exists as server component with all 8 sections: Hero, Pain Point, Value Proposition, Step Guide, Latest Jobs, Filter Categories, FAQ, and Final CTA. Page fetches latest jobs server-side with ISR. Job data is server-rendered (visible in HTML source).</done>
</task>

<task type="auto">
  <name>Task 2: Create seeker FAQ client component</name>
  <files>apps/web/components/info-pages/seeker-faq-section.tsx</files>
  <action>
Create `apps/web/components/info-pages/seeker-faq-section.tsx` as a client component.

This is a dedicated FAQ section for the job seekers page with 6 seeker-specific Q&As.

Structure:
- 'use client' directive
- Import Accordion components from `@/components/ui/accordion` (confirmed: `@/` alias is configured in apps/web/tsconfig.json as `"./*"`, matching existing usage in faq-section.tsx)
- Import motion from `motion/react`
- Section heading: "자주 묻는 질문" (centered, text-3xl md:text-4xl font-bold)
- Subtitle: "Job seekers frequently asked questions"
- Accordion type="single" collapsible
- Background: bg-slate-50, py-20, max-w-3xl mx-auto px-6

FAQ content (6 items):
1. Q: "HangulJobs는 무료인가요?"
   A: "네, 완전 무료입니다. 잡 검색, 프로필 생성, 공고 열람, 관심 표시까지 모든 기능을 무료로 이용하실 수 있습니다."
2. Q: "한국어 능력이 어느 정도여야 하나요?"
   A: "각 공고마다 요구되는 한국어 레벨이 다릅니다. 초급(Basic)부터 원어민(Native) 수준까지 다양한 기회가 있으니, 필터를 활용해 본인에게 맞는 공고를 찾아보세요."
3. Q: "해외 거주자도 이용 가능한가요?"
   A: "네! HangulJobs는 전 세계 어디서든 이용 가능합니다. 원격 근무(Remote), 현지 채용(On-site), 하이브리드(Hybrid) 등 다양한 근무 형태의 공고가 있습니다."
4. Q: "어떤 종류의 일자리가 있나요?"
   A: "IT/개발, 마케팅, 교육/강의, 번역/통역, 무역/물류, 호텔/관광 등 20개 이상의 카테고리에서 정규직, 파트타임, 프리랜서, 인턴십 등 다양한 고용 형태의 공고를 찾으실 수 있습니다."
5. Q: "지원은 어떻게 하나요?"
   A: "공고 상세 페이지에서 '지원하기' 버튼을 클릭하면 고용주가 설정한 지원 방법(이메일 또는 외부 지원 링크)으로 연결됩니다. 관심 있는 공고에 하트를 눌러 나중에 다시 확인할 수도 있습니다."
6. Q: "새로운 공고 알림을 받을 수 있나요?"
   A: "랜딩 페이지 하단에서 뉴스레터를 구독하시면 새로운 공고 알림을 이메일로 받으실 수 있습니다. 구직자 유형을 선택하면 맞춤형 정보를 받아보실 수 있습니다."

Follow the exact styling pattern from the existing `faq-section.tsx` in landing/ (AccordionTrigger with text-left text-base md:text-lg font-semibold, AccordionContent with text-gray-600 leading-relaxed).
  </action>
  <verify>`npx tsc --noEmit -p apps/web/tsconfig.json` passes. File exports SeekerFAQSection component.</verify>
  <done>SeekerFAQSection client component exists with 6 job-seeker-specific FAQ items in Radix Accordion format.</done>
</task>

</tasks>

<verification>
1. `apps/web/app/(marketing)/job-seekers/page.tsx` exists and is a server component
2. Page exports metadata with unique title/description/OG tags
3. Page exports revalidate = 7200
4. Page imports and renders: PainPointSection, ValuePropositionSection, StepGuideSection(seeker), PreviewSection, FilterCategoryCards, SeekerFAQSection, FinalCTASection(seeker), Footer
5. Page fetches latest 8 jobs from Supabase server-side
6. `npx tsc --noEmit` passes
7. (marketing) route group created (directory exists, no layout.tsx inside it)
</verification>

<success_criteria>
- /job-seekers page renders all 8 sections from the success criteria
- Server-side data fetching for latest jobs works
- ISR enabled with 2-hour revalidation
- Unique SEO metadata for the page
- SeekerFAQSection has 6 relevant FAQ items
</success_criteria>

<output>
After completion, create `.planning/phases/14-info-pages/14-02-SUMMARY.md`
</output>
